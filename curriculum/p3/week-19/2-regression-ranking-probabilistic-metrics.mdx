---
title: "Regression, ranking & probabilistic metrics"
description: "Week 19 · Module 2 — taught at three depths, with a lab."
icon: "pen-line"
---
{/* AUTHORED */}

{/* HERO:BEGIN generated by tools/covers_lib.py */}
<Frame>
  <div className="block dark:hidden w-full">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 340" width="100%" role="img" aria-labelledby="t-cover-w19m2 d-cover-w19m2" font-family="'Caveat', 'Kalam', cursive, sans-serif"> <title id="t-cover-w19m2">cover-w19m2</title> <desc id="d-cover-w19m2">Squaring errors makes one four-hour delay dominate a thousand five-minute ones; ranking measures weight the top of the list; calibration asks whether 70% happens 70% of the time.</desc> <style>{`@import url('https://fonts.googleapis.com/css2?family=Caveat:wght@400;700&amp;family=Kalam:wght@300;400;700&amp;display=swap');text{font-family:'Caveat', 'Kalam', cursive, sans-serif;}`}</style> <rect width="1000" height="340" fill="#fffdf7"/> <path d="M54.2,51.4 C55,52.5 57.8,55.7 58.9,58.2 C59.9,60.8 61.3,64.4 60.5,66.9 C59.7,69.4 56.6,72.2 54.1,73.2 C51.6,74.3 48.3,73.7 45.7,73.1 C43,72.5 39.8,71.7 38.1,69.7 C36.4,67.8 35.5,64.4 35.4,61.5 C35.2,58.5 35.3,53.8 37.1,52 C39,50.1 43.4,50.7 46.6,50.4 C49.7,50 53.7,48.5 56,49.8 C58.4,51.1 60,56.9 60.8,58.3" fill="none" stroke="#f08c00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M60.5,67.7 C59.4,69 57,74.3 54.4,75.5 C51.7,76.7 47.2,75.9 44.6,74.8 C41.9,73.6 39.8,71.1 38.3,68.7 C36.7,66.4 35.1,63.6 35,60.7 C34.9,57.8 35.8,53.2 37.8,51.4 C39.9,49.7 44.2,50 47.2,50 C50.3,49.9 53.6,49.8 56,51.3 C58.4,52.8 61,56.1 61.5,58.9 C62.1,61.6 60.9,65.4 59.5,67.9 C58,70.4 53.9,72.8 52.8,73.7" fill="none" stroke="#f08c00" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.1 -0.2)"/> <text x="48" y="67.5" font-size="23.6" fill="#f08c00" text-anchor="middle" font-weight="700">1</text> <text x="180" y="62" font-size="21.8" fill="#1e1e1e" text-anchor="middle" transform="rotate(0.1 180 62)">1,000 orders wrong by 5 min</text> <text x="180" y="88" font-size="21.8" fill="#e03131" text-anchor="middle" transform="rotate(-0.5 180 88)">1 order wrong by 4 hours</text> <text x="180" y="128" font-size="23.2" fill="#f08c00" text-anchor="middle" transform="rotate(0.5 180 128)">should that one dominate?</text> <text x="180" y="164" font-size="21.8" fill="#6b6b6b" text-anchor="middle" transform="rotate(-0.2 180 164)">square the errors → yes</text> <text x="180" y="186" font-size="21.8" fill="#6b6b6b" text-anchor="middle" transform="rotate(0.2 180 186)">treat them evenly → no</text> <path d="M388.6,54.5 C389.3,55.7 393,59.1 393.3,61.8 C393.6,64.5 392.2,68.1 390.4,70.6 C388.7,73 385.6,75.9 382.6,76.5 C379.7,77.2 375.4,76 372.9,74.3 C370.3,72.6 368.2,69.3 367.4,66.5 C366.7,63.7 367.5,60.3 368.5,57.7 C369.6,55 371.4,52.3 373.8,50.7 C376.1,49.1 380.3,47.5 382.8,48.2 C385.3,48.9 386.8,52.7 388.7,55.1 C390.7,57.5 393.4,61.3 394.3,62.5" fill="none" stroke="#f08c00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M382.3,74.2 C380.8,74.1 375.8,75 373.4,73.7 C370.9,72.5 368.5,69.4 367.4,66.6 C366.3,63.9 365.8,59.6 366.9,57.2 C368.1,54.8 371.8,53.6 374.4,52.2 C377,50.7 379.8,48.1 382.6,48.3 C385.4,48.4 389.7,50.6 391.2,52.9 C392.8,55.3 392.3,59.2 392.2,62.3 C392,65.4 392.2,69.2 390.5,71.4 C388.8,73.5 384.6,75.2 381.8,75.3 C379,75.3 375.1,72.4 373.7,71.9" fill="none" stroke="#f08c00" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.9 -0.8)"/> <text x="380" y="67.5" font-size="23.6" fill="#f08c00" text-anchor="middle" font-weight="700">2</text> <path d="M474.9,51.8 C472.1,52.2 470.5,54.8 468.9,56.7 M484.5,51.4 C479.1,56.4 476.1,63.5 469.8,66.3 M494.6,51.4 C487.7,58.1 481.8,64.9 477.1,68.6 M503.2,51.1 C497.7,57.5 491.8,62.9 486.7,67.7 M512.4,51.7 C507.3,57.9 501.4,63.2 495.6,68.8 M520.1,51.3 C515.3,56.1 507.7,65.2 503.2,68.3 M530,51.5 C525.6,53.9 516.6,62.4 512.9,68.5 M537.4,51.7 C532.7,56.7 527.6,63.3 521.1,67.9 M545.7,50.8 C541.3,56.3 532.7,63.8 528.4,68.3 M552.3,52.2 C545.6,55 541.2,62.8 535.3,68.1 M561.5,50.7 C558.5,53.8 548.7,64.3 544.7,68.8 M570.5,51.8 C567.2,55.7 559.6,64.2 553.4,68.9 M580.8,51.2 C575,58.7 568,66.3 562.9,68.3 M589.1,51.2 C584.2,57.9 578.2,62.8 572.5,68.2 M597.9,51.8 C595.1,54.8 584.7,64 580.6,68.2 M605.8,51.5 C600.3,55.5 594.2,63.9 589.1,67.8 M615.4,51.2 C608.9,56.4 603.3,63.4 597.8,68.2 M622.4,51.3 C617,57.4 611.4,65.1 605.4,68.2 M632,52.1 C627.7,54.5 620.9,65.3 615.5,68.1 M640.1,51.8 C637.2,59.3 628.4,64.3 623.4,68.5 M631.2,68 C633.8,65.7 638.7,61.8 640.6,59.2" fill="none" stroke="#2f9e44" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M468.7,52.4 C511.5,51.8 586.9,53.3 640.3,52.4 M639.9,49.9 C639.8,56.2 639.8,63.1 639.6,68.8 M642.2,68.4 C587,67.8 525.6,68.6 468.1,67.9 M469.9,70.5 C469.7,64.7 470.3,57.4 470,51.1" fill="none" stroke="#2f9e44" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M469.3,52.1 C535.3,52.1 572.8,51.4 640.8,52.3 M640.4,51.9 C641.4,56.5 641.4,65.9 640.4,70.6 M640.4,67.5 C594.4,68 517.7,67.8 469.3,67.8 M470.2,69.1 C468.7,65 469.2,58 470,50" fill="none" stroke="#2f9e44" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-1.2 0.9)"/> <text x="462" y="64" font-size="21.8" fill="#1e1e1e" text-anchor="end">position 1</text> <text x="648" y="64" font-size="21.8" fill="#2f9e44" text-anchor="start" font-weight="700">counts</text> <path d="M474.9,83.3 C472.7,85.4 472,88.1 469.4,88.6 M482.5,83.3 C478.6,88.7 473.4,91.4 469.7,95.6 M490.7,83 C484.4,88.6 478.4,94.9 472.9,100.1 M498.4,83.4 C494.4,88.2 486.4,94.5 480.7,100.4 M507.7,83.9 C503.1,90.1 497.8,93.9 490.8,100.4 M515.9,83.9 C508.8,91 503.3,94.7 499.9,100.4 M524.4,83.7 C518.1,87.4 511.8,93.1 507.5,100.4 M516.1,99.9 C520.5,95.9 526.7,91.1 530,85.9 M524.9,100.4 C526.3,100.1 529.4,96.8 530,95" fill="none" stroke="#f08c00" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M467.8,84.2 C483.9,85.4 510.4,83.1 531,83.7 M529.7,84.3 C530.5,87.9 529.6,99 529.7,102.2 M531.4,99.5 C519.6,99.9 482,101 468.8,99.9 M469.9,101.4 C470.1,95.8 470.1,90.4 469.7,83.3" fill="none" stroke="#f08c00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M467.4,83.6 C483.6,84.7 513.8,83 531.8,83.5 M530.5,81.3 C531.4,87.5 530.4,93.1 529.5,100.9 M532,100.4 C509.1,99.2 481.5,101 467.9,100.3 M470.4,100.7 C469.1,94.8 469.6,89 469.6,81.7" fill="none" stroke="#f08c00" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-1 -1.3)"/> <text x="462" y="96" font-size="21.8" fill="#1e1e1e" text-anchor="end">position 5</text> <text x="538" y="96" font-size="21.8" fill="#f08c00" text-anchor="start" font-weight="700">a bit</text> <path d="M474.5,115.5 C472.4,117.6 470.4,119.4 469.7,120.8 M469.5,129.3 C472.1,127.2 476.4,123.6 478.7,119.7 M474.6,132 C475.4,130.3 478.1,130.3 478.7,127.7" fill="none" stroke="#e03131" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M468.3,115.8 C469.8,114.8 475.2,115.9 477.8,115.7 M477.5,114.4 C477.5,118.3 478.2,127.4 477.6,133.3 M478.8,132.5 C475.6,131.3 473.2,131.6 468.5,131.5 M470.2,133.7 C470.4,127.5 469.4,121.7 469.7,114.3" fill="none" stroke="#e03131" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M468.5,115.6 C472.5,115.9 474.4,115.9 479.3,115.7 M478.4,114.5 C479.2,119.4 479.3,127.1 477.5,133.9 M480.9,131.5 C476.8,131.2 473.3,133.4 469.3,131.7 M469.9,132.9 C470.6,126.4 469.8,118.4 469.9,116" fill="none" stroke="#e03131" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(0.5 -0.8)"/> <text x="462" y="128" font-size="21.8" fill="#1e1e1e" text-anchor="end">position 40</text> <text x="486" y="128" font-size="21.8" fill="#e03131" text-anchor="start" font-weight="700">nobody scrolls</text> <text x="520" y="166" font-size="23.2" fill="#6b6b6b" text-anchor="middle" transform="rotate(-0.5 520 166)">ranking is graded at the top</text> <path d="M680.5,191.7 C681.7,190.9 685.3,187.3 688,186.9 C690.7,186.5 694.4,187.6 696.7,189.1 C699.1,190.6 701.5,193.4 702.3,196 C703.1,198.6 702.6,202 701.7,204.8 C700.7,207.6 699.2,211.6 696.7,213.1 C694.3,214.5 689.9,214.1 686.9,213.3 C683.9,212.5 680.6,210.7 678.8,208.3 C676.9,206 675.3,201.8 675.9,199.1 C676.4,196.4 679.8,194.4 681.9,192.2 C684,190 687.5,186.8 688.6,185.7" fill="none" stroke="#f08c00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M701.5,200.7 C700.9,201.8 699.8,205.9 698.1,207.7 C696.4,209.5 693.8,211.1 691.2,211.6 C688.7,212 685.4,211.7 683,210.3 C680.6,209 677.7,206.3 676.9,203.6 C676.1,201 676.7,196.8 678.1,194.5 C679.5,192.2 682.8,190.8 685.3,189.9 C687.8,188.9 690.7,188.2 693.1,188.7 C695.5,189.3 698.3,191.3 699.8,193.4 C701.3,195.4 702,198.6 701.9,201.3 C701.8,204 699.5,208.1 699,209.5" fill="none" stroke="#f08c00" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.4 0.5)"/> <text x="690" y="205.5" font-size="23.6" fill="#f08c00" text-anchor="middle" font-weight="700">3</text> <text x="850" y="212" font-size="24.6" fill="#f08c00" text-anchor="middle" transform="rotate(-0.6 850 212)">“70% likely to churn”</text> <text x="850" y="240" font-size="21.8" fill="#6b6b6b" text-anchor="middle" transform="rotate(0.8 850 240)">do 70 of every 100 actually leave?</text> <text x="850" y="264" font-size="21.8" fill="#e03131" text-anchor="middle" transform="rotate(-0.4 850 264)">if not, it is a mood, not a probability</text> <text x="200" y="250" font-size="27.6" fill="#1e1e1e" text-anchor="middle" font-weight="700">calibration</text> <path d="M131.8,253.7 C129.6,251.9 117.6,246.4 118.4,242.8 C119.3,239.2 126.7,235.1 137.1,232.3 C147.4,229.4 164.4,226.7 180.5,225.7 C196.6,224.8 218.5,225.3 233.7,226.8 C248.8,228.3 263.6,231.6 271.4,234.8 C279.2,238 282.1,242.3 280.5,245.9 C279,249.6 272.9,254 262,256.7 C251,259.3 231.4,261.3 214.9,261.9 C198.3,262.5 177.4,262.1 162.9,260.4 C148.3,258.8 134.8,255.3 127.6,252.1 C120.3,248.8 116.9,244.5 119.3,240.9 C121.8,237.4 138.4,232.4 142.2,230.7" fill="none" stroke="#f08c00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/> <path d="M187.8,261.2 C180,260.5 152.5,259.2 141,256.7 C129.5,254.2 121,250 118.9,246.4 C116.9,242.9 120.8,238.5 128.7,235.4 C136.6,232.3 151.8,229.4 166.4,228 C181,226.5 200.6,225.9 216.5,226.6 C232.4,227.2 251.3,229.2 261.9,231.8 C272.5,234.5 278.7,238.8 280.2,242.4 C281.7,246 279,250.4 270.6,253.5 C262.3,256.5 245.5,259.3 230.3,260.6 C215.1,261.8 195.5,261.8 179.5,261 C163.5,260.2 144.5,258.2 134.2,255.5 C124,252.7 120.7,246.4 118,244.6" fill="none" stroke="#f08c00" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(1 0.9)"/> <text x="330" y="219.5" font-size="21.8" fill="#f08c00" text-anchor="start" transform="rotate(-2.3 330 219.5)">the choice of yardstick IS the</text> <text x="330" y="244.5" font-size="21.8" fill="#f08c00" text-anchor="start" transform="rotate(-2.3 330 244.5)">business decision.</text> <text x="600" y="290.2" font-size="27.6" fill="#f08c00" text-anchor="middle" transform="rotate(-0.6 600 290.2)">and which data gets to</text> <text x="600" y="321.8" font-size="27.6" fill="#f08c00" text-anchor="middle" transform="rotate(-0.6 600 321.8)">grade you at all?</text> <path d="M150 300 L340 300" fill="none" stroke="#f08c00" stroke-width="3.5" stroke-dasharray="12 9" opacity="0.9"><animate attributeName="stroke-dashoffset" from="42" to="0" dur="1.6s" repeatCount="indefinite"/></path></svg>
  </div>
  <div className="hidden dark:block w-full">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 340" width="100%" role="img" aria-labelledby="t-cover-w19m2 d-cover-w19m2" font-family="'Caveat', 'Kalam', cursive, sans-serif"> <title id="t-cover-w19m2">cover-w19m2</title> <desc id="d-cover-w19m2">Squaring errors makes one four-hour delay dominate a thousand five-minute ones; ranking measures weight the top of the list; calibration asks whether 70% happens 70% of the time.</desc> <style>{`@import url('https://fonts.googleapis.com/css2?family=Caveat:wght@400;700&amp;family=Kalam:wght@300;400;700&amp;display=swap');text{font-family:'Caveat', 'Kalam', cursive, sans-serif;}`}</style> <rect width="1000" height="340" fill="#1a1a19"/> <path d="M54.2,51.4 C55,52.5 57.8,55.7 58.9,58.2 C59.9,60.8 61.3,64.4 60.5,66.9 C59.7,69.4 56.6,72.2 54.1,73.2 C51.6,74.3 48.3,73.7 45.7,73.1 C43,72.5 39.8,71.7 38.1,69.7 C36.4,67.8 35.5,64.4 35.4,61.5 C35.2,58.5 35.3,53.8 37.1,52 C39,50.1 43.4,50.7 46.6,50.4 C49.7,50 53.7,48.5 56,49.8 C58.4,51.1 60,56.9 60.8,58.3" fill="none" stroke="#ffb84d" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M60.5,67.7 C59.4,69 57,74.3 54.4,75.5 C51.7,76.7 47.2,75.9 44.6,74.8 C41.9,73.6 39.8,71.1 38.3,68.7 C36.7,66.4 35.1,63.6 35,60.7 C34.9,57.8 35.8,53.2 37.8,51.4 C39.9,49.7 44.2,50 47.2,50 C50.3,49.9 53.6,49.8 56,51.3 C58.4,52.8 61,56.1 61.5,58.9 C62.1,61.6 60.9,65.4 59.5,67.9 C58,70.4 53.9,72.8 52.8,73.7" fill="none" stroke="#ffb84d" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.1 -0.2)"/> <text x="48" y="67.5" font-size="23.6" fill="#ffb84d" text-anchor="middle" font-weight="700">1</text> <text x="180" y="62" font-size="21.8" fill="#e8e6e3" text-anchor="middle" transform="rotate(0.1 180 62)">1,000 orders wrong by 5 min</text> <text x="180" y="88" font-size="21.8" fill="#ff6b6b" text-anchor="middle" transform="rotate(-0.5 180 88)">1 order wrong by 4 hours</text> <text x="180" y="128" font-size="23.2" fill="#ffb84d" text-anchor="middle" transform="rotate(0.5 180 128)">should that one dominate?</text> <text x="180" y="164" font-size="21.8" fill="#9a9791" text-anchor="middle" transform="rotate(-0.2 180 164)">square the errors → yes</text> <text x="180" y="186" font-size="21.8" fill="#9a9791" text-anchor="middle" transform="rotate(0.2 180 186)">treat them evenly → no</text> <path d="M388.6,54.5 C389.3,55.7 393,59.1 393.3,61.8 C393.6,64.5 392.2,68.1 390.4,70.6 C388.7,73 385.6,75.9 382.6,76.5 C379.7,77.2 375.4,76 372.9,74.3 C370.3,72.6 368.2,69.3 367.4,66.5 C366.7,63.7 367.5,60.3 368.5,57.7 C369.6,55 371.4,52.3 373.8,50.7 C376.1,49.1 380.3,47.5 382.8,48.2 C385.3,48.9 386.8,52.7 388.7,55.1 C390.7,57.5 393.4,61.3 394.3,62.5" fill="none" stroke="#ffb84d" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M382.3,74.2 C380.8,74.1 375.8,75 373.4,73.7 C370.9,72.5 368.5,69.4 367.4,66.6 C366.3,63.9 365.8,59.6 366.9,57.2 C368.1,54.8 371.8,53.6 374.4,52.2 C377,50.7 379.8,48.1 382.6,48.3 C385.4,48.4 389.7,50.6 391.2,52.9 C392.8,55.3 392.3,59.2 392.2,62.3 C392,65.4 392.2,69.2 390.5,71.4 C388.8,73.5 384.6,75.2 381.8,75.3 C379,75.3 375.1,72.4 373.7,71.9" fill="none" stroke="#ffb84d" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.9 -0.8)"/> <text x="380" y="67.5" font-size="23.6" fill="#ffb84d" text-anchor="middle" font-weight="700">2</text> <path d="M474.9,51.8 C472.1,52.2 470.5,54.8 468.9,56.7 M484.5,51.4 C479.1,56.4 476.1,63.5 469.8,66.3 M494.6,51.4 C487.7,58.1 481.8,64.9 477.1,68.6 M503.2,51.1 C497.7,57.5 491.8,62.9 486.7,67.7 M512.4,51.7 C507.3,57.9 501.4,63.2 495.6,68.8 M520.1,51.3 C515.3,56.1 507.7,65.2 503.2,68.3 M530,51.5 C525.6,53.9 516.6,62.4 512.9,68.5 M537.4,51.7 C532.7,56.7 527.6,63.3 521.1,67.9 M545.7,50.8 C541.3,56.3 532.7,63.8 528.4,68.3 M552.3,52.2 C545.6,55 541.2,62.8 535.3,68.1 M561.5,50.7 C558.5,53.8 548.7,64.3 544.7,68.8 M570.5,51.8 C567.2,55.7 559.6,64.2 553.4,68.9 M580.8,51.2 C575,58.7 568,66.3 562.9,68.3 M589.1,51.2 C584.2,57.9 578.2,62.8 572.5,68.2 M597.9,51.8 C595.1,54.8 584.7,64 580.6,68.2 M605.8,51.5 C600.3,55.5 594.2,63.9 589.1,67.8 M615.4,51.2 C608.9,56.4 603.3,63.4 597.8,68.2 M622.4,51.3 C617,57.4 611.4,65.1 605.4,68.2 M632,52.1 C627.7,54.5 620.9,65.3 615.5,68.1 M640.1,51.8 C637.2,59.3 628.4,64.3 623.4,68.5 M631.2,68 C633.8,65.7 638.7,61.8 640.6,59.2" fill="none" stroke="#6cc47a" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M468.7,52.4 C511.5,51.8 586.9,53.3 640.3,52.4 M639.9,49.9 C639.8,56.2 639.8,63.1 639.6,68.8 M642.2,68.4 C587,67.8 525.6,68.6 468.1,67.9 M469.9,70.5 C469.7,64.7 470.3,57.4 470,51.1" fill="none" stroke="#6cc47a" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M469.3,52.1 C535.3,52.1 572.8,51.4 640.8,52.3 M640.4,51.9 C641.4,56.5 641.4,65.9 640.4,70.6 M640.4,67.5 C594.4,68 517.7,67.8 469.3,67.8 M470.2,69.1 C468.7,65 469.2,58 470,50" fill="none" stroke="#6cc47a" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-1.2 0.9)"/> <text x="462" y="64" font-size="21.8" fill="#e8e6e3" text-anchor="end">position 1</text> <text x="648" y="64" font-size="21.8" fill="#6cc47a" text-anchor="start" font-weight="700">counts</text> <path d="M474.9,83.3 C472.7,85.4 472,88.1 469.4,88.6 M482.5,83.3 C478.6,88.7 473.4,91.4 469.7,95.6 M490.7,83 C484.4,88.6 478.4,94.9 472.9,100.1 M498.4,83.4 C494.4,88.2 486.4,94.5 480.7,100.4 M507.7,83.9 C503.1,90.1 497.8,93.9 490.8,100.4 M515.9,83.9 C508.8,91 503.3,94.7 499.9,100.4 M524.4,83.7 C518.1,87.4 511.8,93.1 507.5,100.4 M516.1,99.9 C520.5,95.9 526.7,91.1 530,85.9 M524.9,100.4 C526.3,100.1 529.4,96.8 530,95" fill="none" stroke="#ffb84d" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M467.8,84.2 C483.9,85.4 510.4,83.1 531,83.7 M529.7,84.3 C530.5,87.9 529.6,99 529.7,102.2 M531.4,99.5 C519.6,99.9 482,101 468.8,99.9 M469.9,101.4 C470.1,95.8 470.1,90.4 469.7,83.3" fill="none" stroke="#ffb84d" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M467.4,83.6 C483.6,84.7 513.8,83 531.8,83.5 M530.5,81.3 C531.4,87.5 530.4,93.1 529.5,100.9 M532,100.4 C509.1,99.2 481.5,101 467.9,100.3 M470.4,100.7 C469.1,94.8 469.6,89 469.6,81.7" fill="none" stroke="#ffb84d" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-1 -1.3)"/> <text x="462" y="96" font-size="21.8" fill="#e8e6e3" text-anchor="end">position 5</text> <text x="538" y="96" font-size="21.8" fill="#ffb84d" text-anchor="start" font-weight="700">a bit</text> <path d="M474.5,115.5 C472.4,117.6 470.4,119.4 469.7,120.8 M469.5,129.3 C472.1,127.2 476.4,123.6 478.7,119.7 M474.6,132 C475.4,130.3 478.1,130.3 478.7,127.7" fill="none" stroke="#ff6b6b" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.8"/> <path d="M468.3,115.8 C469.8,114.8 475.2,115.9 477.8,115.7 M477.5,114.4 C477.5,118.3 478.2,127.4 477.6,133.3 M478.8,132.5 C475.6,131.3 473.2,131.6 468.5,131.5 M470.2,133.7 C470.4,127.5 469.4,121.7 469.7,114.3" fill="none" stroke="#ff6b6b" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M468.5,115.6 C472.5,115.9 474.4,115.9 479.3,115.7 M478.4,114.5 C479.2,119.4 479.3,127.1 477.5,133.9 M480.9,131.5 C476.8,131.2 473.3,133.4 469.3,131.7 M469.9,132.9 C470.6,126.4 469.8,118.4 469.9,116" fill="none" stroke="#ff6b6b" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(0.5 -0.8)"/> <text x="462" y="128" font-size="21.8" fill="#e8e6e3" text-anchor="end">position 40</text> <text x="486" y="128" font-size="21.8" fill="#ff6b6b" text-anchor="start" font-weight="700">nobody scrolls</text> <text x="520" y="166" font-size="23.2" fill="#9a9791" text-anchor="middle" transform="rotate(-0.5 520 166)">ranking is graded at the top</text> <path d="M680.5,191.7 C681.7,190.9 685.3,187.3 688,186.9 C690.7,186.5 694.4,187.6 696.7,189.1 C699.1,190.6 701.5,193.4 702.3,196 C703.1,198.6 702.6,202 701.7,204.8 C700.7,207.6 699.2,211.6 696.7,213.1 C694.3,214.5 689.9,214.1 686.9,213.3 C683.9,212.5 680.6,210.7 678.8,208.3 C676.9,206 675.3,201.8 675.9,199.1 C676.4,196.4 679.8,194.4 681.9,192.2 C684,190 687.5,186.8 688.6,185.7" fill="none" stroke="#ffb84d" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/> <path d="M701.5,200.7 C700.9,201.8 699.8,205.9 698.1,207.7 C696.4,209.5 693.8,211.1 691.2,211.6 C688.7,212 685.4,211.7 683,210.3 C680.6,209 677.7,206.3 676.9,203.6 C676.1,201 676.7,196.8 678.1,194.5 C679.5,192.2 682.8,190.8 685.3,189.9 C687.8,188.9 690.7,188.2 693.1,188.7 C695.5,189.3 698.3,191.3 699.8,193.4 C701.3,195.4 702,198.6 701.9,201.3 C701.8,204 699.5,208.1 699,209.5" fill="none" stroke="#ffb84d" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(-0.4 0.5)"/> <text x="690" y="205.5" font-size="23.6" fill="#ffb84d" text-anchor="middle" font-weight="700">3</text> <text x="850" y="212" font-size="24.6" fill="#ffb84d" text-anchor="middle" transform="rotate(-0.6 850 212)">“70% likely to churn”</text> <text x="850" y="240" font-size="21.8" fill="#9a9791" text-anchor="middle" transform="rotate(0.8 850 240)">do 70 of every 100 actually leave?</text> <text x="850" y="264" font-size="21.8" fill="#ff6b6b" text-anchor="middle" transform="rotate(-0.4 850 264)">if not, it is a mood, not a probability</text> <text x="200" y="250" font-size="27.6" fill="#e8e6e3" text-anchor="middle" font-weight="700">calibration</text> <path d="M131.8,253.7 C129.6,251.9 117.6,246.4 118.4,242.8 C119.3,239.2 126.7,235.1 137.1,232.3 C147.4,229.4 164.4,226.7 180.5,225.7 C196.6,224.8 218.5,225.3 233.7,226.8 C248.8,228.3 263.6,231.6 271.4,234.8 C279.2,238 282.1,242.3 280.5,245.9 C279,249.6 272.9,254 262,256.7 C251,259.3 231.4,261.3 214.9,261.9 C198.3,262.5 177.4,262.1 162.9,260.4 C148.3,258.8 134.8,255.3 127.6,252.1 C120.3,248.8 116.9,244.5 119.3,240.9 C121.8,237.4 138.4,232.4 142.2,230.7" fill="none" stroke="#ffb84d" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/> <path d="M187.8,261.2 C180,260.5 152.5,259.2 141,256.7 C129.5,254.2 121,250 118.9,246.4 C116.9,242.9 120.8,238.5 128.7,235.4 C136.6,232.3 151.8,229.4 166.4,228 C181,226.5 200.6,225.9 216.5,226.6 C232.4,227.2 251.3,229.2 261.9,231.8 C272.5,234.5 278.7,238.8 280.2,242.4 C281.7,246 279,250.4 270.6,253.5 C262.3,256.5 245.5,259.3 230.3,260.6 C215.1,261.8 195.5,261.8 179.5,261 C163.5,260.2 144.5,258.2 134.2,255.5 C124,252.7 120.7,246.4 118,244.6" fill="none" stroke="#ffb84d" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" stroke-opacity="0.5" transform="translate(1 0.9)"/> <text x="330" y="219.5" font-size="21.8" fill="#ffb84d" text-anchor="start" transform="rotate(-2.3 330 219.5)">the choice of yardstick IS the</text> <text x="330" y="244.5" font-size="21.8" fill="#ffb84d" text-anchor="start" transform="rotate(-2.3 330 244.5)">business decision.</text> <text x="600" y="290.2" font-size="27.6" fill="#ffb84d" text-anchor="middle" transform="rotate(-0.6 600 290.2)">and which data gets to</text> <text x="600" y="321.8" font-size="27.6" fill="#ffb84d" text-anchor="middle" transform="rotate(-0.6 600 321.8)">grade you at all?</text> <path d="M150 300 L340 300" fill="none" stroke="#ffb84d" stroke-width="3.5" stroke-dasharray="12 9" opacity="0.9"><animate attributeName="stroke-dashoffset" from="42" to="0" dur="1.6s" repeatCount="indefinite"/></path></svg>
  </div>
</Frame>
{/* HERO:END */}

<Note>
**Week 19 · Module 2 · `P3-W19-M2`** — start with 🟢 Ground if this page's words are not
second nature yet — even after years of shipping code. Move to 🔵 Build once they are, and 🟣 Edge
when Build has already hurt you. Nobody skips Ground the first time through.
</Note>

<Card title="In this chapter — about 25 minutes" icon="sparkles">
  - **Being wrong by £5 versus being wrong by £5,000** — which loss you pick decides
  - **Ranking is graded at the top** — nobody scrolls to result 40
  - **Is 70% confidence really 70%?** — calibration, and why it pays your rent
  - **Pick the right measure for the job** — and say why
</Card>

<Info>
**Where the numbers on this page come from.** Every table introduced by *Measured* was produced by
running the code shown, on an Apple M3 Pro (11 cores, 18 GB, macOS arm64, **no GPU**) with
Python 3.12.9 and numpy 2.5.2, scikit-learn 1.9.0, `xgboost` 2.1.4 — the same environment as the
rest of Phase 3, so you can re-run any of it. The datasets are the public telco-churn and
California-housing sets plus synthetic draws whose generating parameters are given in the code.
Anything introduced by *Derived* is arithmetic, shown term by term — the split-conformal coverage
guarantee is a property of the procedure, not of this dataset. A result attributed to a paper is
that paper's measurement, not ours. See [About the numbers](/start/about-the-numbers).
</Info>


## What you'll be able to do

1. Compute MAE, MSE, RMSE, median absolute error and MAPE by hand on ten predictions, and say which one moves 69× when a single prediction goes wrong
2. Show, numerically, that minimising MAPE makes you under-forecast by **61%** on lognormal demand — and know when to reach for it anyway
3. Report R-squared honestly, using the measured case where 1,000 noise features raise training R-squared by 0.024 while held-out R-squared falls
4. Compute **Recall@k, Precision@k, MRR and NDCG@k** from a ranked list by hand, using the exact definitions Week 27 Module 5 grades retrieval with
5. Build a reliability diagram, read an expected calibration error, and fix a miscalibrated booster with Platt scaling or isotonic regression — measured at a **7.3× ECE reduction**
6. Produce a prediction interval with guaranteed coverage using split conformal prediction, and explain why the quantile-regression interval under-covered at 0.8653

<Frame caption="The whole idea on one whiteboard. Redraw it from memory before you move on.">
  <img src="/assets/diagrams/p3-w19-m2.svg" alt="Hand-drawn sketch: Regression, ranking & probabilistic metrics" className="block dark:hidden" />
  <img src="/assets/diagrams/p3-w19-m2-dark.svg" alt="Hand-drawn sketch: Regression, ranking & probabilistic metrics" className="hidden dark:block" />
</Frame>

{/* DIAGRAM-SPEC
id: p3-w19-m2
tier: T2
one-sentence-goal: Show that a metric is a claim about which errors you are willing to trade for which, drawn as three loss shapes over the error axis and one ranked list where the same recall hides two very different user experiences.
elements:
  - left panel: one horizontal error axis from -100 to +100 with three curves drawn over it in different colours — a V for absolute error labelled "MAE -> median", a parabola for squared error labelled "MSE -> mean", and a tilted V for pinball loss at q=0.9 labelled "quantile -> the 90th percentile"
  - amber margin note under the parabola: "one bad row moved MSE 69x and median-AE not at all. that IS the metric choosing for you."
  - a small crossed-out MAPE curve in red on the same axis, asymmetric, with the note "unbounded above, capped at 100% below -> always under-forecasts. measured: 38.7% of the mean"
  - right panel: two ranked lists of ten boxes side by side, relevant items shaded, list A with hits at ranks 2,3,5 and list B with hits at 1,2,5. Both annotated "recall@5 = 1.00" in green with a circle around the identical numbers
  - below them, in red: "MRR 0.50 vs 1.00" and "NDCG@5 0.7328 vs 0.9469", with a fat arrow and the note "recall@k is blind above k. this is why week 27 reports both."
  - bottom strip: a reliability diagram, the 45-degree line drawn dashed, one wobbly curve sagging far below it labelled "raw booster, ECE 0.1366" and one hugging the line labelled "Platt, ECE 0.0188"
  - a small green box off to the side: "tuned model, no calibration at all: Brier 0.1342" with amber note "fixing the model beat calibrating the broken one"
  - dangling arrow: "...and how wide should the interval be? -> conformal, layer 3"
numbers-to-write-in: MSE x69.1 / MAE x3.55 / medAE x1.00, MAPE-optimal constant 38.7% of the mean, recall@5 1.00 both, MRR 0.50 vs 1.00, NDCG 0.7328 vs 0.9469, ECE 0.1366 -> 0.0188, Brier 0.1752 / 0.1554 / 0.1342
alt: A hand-drawn three-part sketch: three loss-function shapes over an error axis showing that absolute error targets the median while squared error targets the mean, two ranked lists whose identical recall at five hides an MRR of 0.50 against 1.00, and a reliability diagram where a raw booster's curve sags far below the diagonal until Platt scaling pulls it back.
*/}



<Tabs>
  <Tab title="🟢 Layer 1 — Ground">

{/* STORY */}
{/* TERM LADDER: MAE → RMSE → NDCG → calibration → Brier score */}

### Three jobs, three yardsticks

A model that predicts a number, a model that ranks a list, and a model that states a
probability, cannot possibly be graded the same way.

**Predicting a number.** Your delivery-time model is wrong by five minutes on a thousand orders
and wrong by four hours on one. Should that one disaster dominate the score? If a four-hour
delay costs you a customer, then yes — use a measure that squares the errors. If all errors are
merely annoying, no — use one that treats them evenly. The choice *is* the business.

**Ranking a list.** A search model puts the right answer at position 40. It is technically
"correct" and practically useless, because nobody scrolls that far. Ranking measures deliberately
weight the top of the list heavily and the tail barely at all.

**Stating a probability.** The subtlest of the three. When your model says *70% likely to
churn*, are 70 out of every 100 such customers actually leaving? If not, the number is a mood,
not a probability — and every decision that multiplies it by a cost is wrong.

### Ten predictions

A **regression** model predicts a number, not a class, so there is no confusion matrix. There is only the gap between what you said and what happened. Here are ten house prices in thousands, truth and prediction:

```
truth y      120  135  150  160  175  180  195  210  225  240
prediction   125  130  148  165  170  185  190  205  230  235
error e       +5   -5   -2   +5   -5   +5   -5   -5   +5   -5
```

The **error** (or **residual**) is `prediction - truth`. Signed, so a positive means you guessed too high. You cannot just average the errors — the pluses and minuses cancel and a model that is wildly wrong in both directions scores zero. So every regression metric is a different way of making the errors positive first.

**Mean absolute error (MAE)** — make each error positive by dropping the sign, then average.

```
MAE = (5 + 5 + 2 + 5 + 5 + 5 + 5 + 5 + 5 + 5) / 10 = 47/10 = 4.70
```

MAE is in the same units as the label — here, thousands of pounds. "On average we are £4,700 out." That is a sentence a stakeholder understands, which is MAE's main advantage.

**Mean squared error (MSE)** — make each error positive by squaring, then average.

```
MSE = (25 + 25 + 4 + 25 + 25 + 25 + 25 + 25 + 25 + 25) / 10 = 229/10 = 22.90
```

The units are thousands *squared*, which means nothing to anyone. So take the square root:

**Root mean squared error (RMSE)** = `sqrt(22.90)` = **4.7854**. Back in thousands of pounds, and always greater than or equal to MAE.

**Median absolute error** — the middle value of the absolute errors rather than their average. Here every absolute error is 5 except one 2, so the median is **5.00**.

**Mean absolute percentage error (MAPE)** — express each error as a fraction of the truth, then average.

```
MAPE = mean(|e_i| / y_i) * 100 = 2.7214 %
```

### The one thing you must see

Change a single prediction. Row 6 — truth 180 — was predicted 185. Suppose the model instead says **305**: one badly wrong row out of ten. Recompute everything.

| Metric | Before | After | Multiplied by |
| --- | --- | --- | --- |
| MAE | 4.7000 | 16.7000 | **3.55×** |
| MSE | 22.9000 | 1582.9000 | **69.12×** |
| RMSE | 4.7854 | 39.7857 | **8.31×** |
| MAPE | 2.7214% | 9.3881% | 3.45× |
| Median absolute error | 5.0000 | 5.0000 | **1.00×** |

One row out of ten moved MSE by a factor of 69 and moved the median absolute error not at all. **That is not a flaw in either metric — it is the metrics doing their jobs.** MSE is designed to scream about large errors; median absolute error is designed to ignore them. Which behaviour you want depends entirely on whether one very wrong prediction is genuinely much worse than several slightly wrong ones. For a delivery ETA, one prediction that is two hours out is much worse. For a heating-demand forecast summed over a thousand homes, it probably is not.

```python
# pip install "numpy==2.5.2"
import numpy as np

y    = np.array([120., 135., 150., 160., 175., 180., 195., 210., 225., 240.])
yhat = np.array([125., 130., 148., 165., 170., 185., 190., 205., 230., 235.])

def report(y, yhat, label):
    e = yhat - y
    print(f"{label:12s} MAE {np.abs(e).mean():8.4f}  MSE {(e**2).mean():10.4f}  "
          f"RMSE {np.sqrt((e**2).mean()):8.4f}  MAPE {np.abs(e/y).mean()*100:7.4f}%  "
          f"medAE {np.median(np.abs(e)):6.4f}")

report(y, yhat, "clean")
bad = yhat.copy(); bad[5] = 305.0
report(y, bad, "one outlier")
```

```
clean        MAE   4.7000  MSE    22.9000  RMSE   4.7854  MAPE  2.7214%  medAE 5.0000
one outlier  MAE  16.7000  MSE  1582.9000  RMSE  39.7857  MAPE  9.3881%  medAE 5.0000
```

### Line by line

- `e = yhat - y` is a NumPy array of ten signed errors; every metric below is one reduction over it.
- `np.abs(e).mean()` is MAE. `(e**2).mean()` is MSE — note that `**` is elementwise, so this squares each error before averaging, not the other way round.
- `np.median(np.abs(e))` is the robust one. It is the only metric in the row that a single bad prediction cannot move, because the median of eleven-ish numbers ignores their extremes by construction.
- `np.abs(e/y)` divides elementwise. If any `y` is zero this produces `inf`, which is MAPE's central problem and the subject of Layer 2.

### Ranking: a different kind of "wrong"

Some models do not predict a number or a class — they **rank**. A search engine returns ten documents; a recommender returns twenty products; in Week 27 a retriever returns the chunks of text that go into a language model's prompt. What matters is not a value, it is *which items appeared and in what order*.

Say for one query there are three genuinely relevant documents, with IDs 3, 5 and 12, and two systems return ten results each:

```
system A:  7   3  12   5   9   1   4   8   2   6
system B:  3  12   7   9   5   4   1   6   8   2
           ^   ^   ^   ^   ^
          r1  r2  r3  r4  r5
```

**Recall@k** — of the relevant documents, what fraction appeared in the top k?

```
A: relevant in top 5 = {3, 12, 5} -> 3/3 = 1.0000
B: relevant in top 5 = {3, 12, 5} -> 3/3 = 1.0000
```

**Identical.** Both systems put all three relevant documents in the top five. **Precision@5** is also identical: 3 of the 5 shown are relevant, so 0.6000 each. If Recall@5 were your only metric, you would report that these two systems are the same.

**MRR (mean reciprocal rank)** — `1 / (rank of the first relevant hit)`, averaged over queries. System A's first hit is at rank 2, so 1/2 = **0.5000**. System B's is at rank 1, so **1.0000**.

**NDCG@k (normalised discounted cumulative gain)** — give each relevant hit a score that shrinks with its position, add them up, and divide by the best possible total. The discount is `1 / log2(i + 2)` where `i` counts from zero, so the first five positions are worth `1.0000, 0.6309, 0.5000, 0.4307, 0.3869`.

```
A hits at positions 2, 3, 5 (i = 1, 2, 4):  0.6309 + 0.5000 + 0.3869 = 1.5178
B hits at positions 1, 2, 5 (i = 0, 1, 4):  1.0000 + 0.6309 + 0.3869 = 2.0178
ideal: three hits at positions 1, 2, 3   :  1.0000 + 0.6309 + 0.5000 = 2.1309

NDCG@5(A) = 1.5178 / 2.1309 = 0.7328
NDCG@5(B) = 2.0178 / 2.1309 = 0.9469
```

Same recall, same precision, and a 0.21 gap in NDCG. If a human reads the first result, that gap is the whole product.

<Note>
**These four functions are exactly the ones Week 27 Module 5 uses to grade a retrieval system**, down to the `1 / log2(i + 2)` discount and the `set` intersection. Weeks 27 and 28 are already written and they assume you learned them here. When you get there, `recall_at_k`, `mrr` and `ndcg_at_k` will appear with no re-derivation — this is the derivation.
</Note>

### Guided practice

1. A third system returns `1 2 4 6 8 10 11 3 5 12` for the same query. Compute Recall@5, Recall@10, MRR and NDCG@10 by hand.
2. In the ten-prediction table, which single metric would you report to a homeowner, and which to a mortgage risk team? Give one sentence each.
3. Compute MAPE for a truth of 100 with a prediction of 50, and again for a truth of 100 with a prediction of 200. Both are "off by a factor of two". Are the two MAPEs equal?

<Accordion title="Answers">
1. Recall@5 = 0/3 = **0.0000** (none of 3, 5, 12 appear in the first five). Recall@10 = 3/3 = **1.0000**. MRR = 1/8 = **0.1250** — the first hit is at rank 8. NDCG@10 = (1/log2(9) + 1/log2(10) + 1/log2(11)) / 2.1309 = (0.3155 + 0.3010 + 0.2891) / 2.1309 = **0.4250**. Read the pair: Recall@10 says this system is perfect and NDCG@10 says it is less than half as good as the ideal. Both are true, and which one you report depends on whether anything downstream reads position — for a language model that receives all ten chunks, Recall@10 is the honest metric; for a human who reads the first, it is not.
2. Homeowner: **MAE**, because "typically £4,700 out" is a sentence they can act on and the units are pounds. Mortgage risk team: **RMSE or the 95th percentile of absolute error**, because their exposure is driven by the rare large misses and MAE deliberately averages those away.
3. No. Predicting 50 gives `|50-100|/100` = **50%**. Predicting 200 gives `|200-100|/100` = **100%**. MAPE is asymmetric: under-prediction can never cost more than 100% while over-prediction is unbounded. Layer 2 shows what that does to a forecast.
</Accordion>

### Checkpoint

<AccordionGroup>
  <Accordion title="1. Why is RMSE always greater than or equal to MAE?">
  Because of Jensen's inequality applied to the convex squaring function: the root-mean-square of a set of non-negative numbers is at least their arithmetic mean, with equality only when every absolute error is identical. In the clean table above, RMSE 4.7854 versus MAE 4.7000 — the small gap tells you the errors are nearly uniform. **A large RMSE/MAE ratio is a free outlier detector**: after the bad row it becomes 39.79/16.70 = 2.38.
  </Accordion>
  <Accordion title="2. If your metric is MAE, what single constant is the best possible prediction?">
  The **median** of the labels. If your metric is MSE, it is the **mean**. This is the fastest way to remember which is which, and it is also the strongest baseline you can build in one line — always report it, exactly as Module 1 reports the majority class.
  </Accordion>
  <Accordion title="3. What does a Recall@10 of 1.0 with an MRR of 0.125 tell you?">
  Everything relevant was found, but the first relevant item was at rank 8. If a person reads the list top-down, they saw seven irrelevant results first. If a language model receives all ten items in its prompt, this system is fine. The metric you report is determined by what consumes the ranking, not by what is easiest to compute.
  </Accordion>
  <Accordion title="4. Why does NDCG need the 'N'?">
  Because raw DCG depends on how many relevant documents exist for that query — a query with eight relevant documents can score a much higher DCG than a query with one, so averaging raw DCG across queries would let easy queries dominate. Dividing by the **ideal** DCG for that query puts every query on a 0-to-1 scale, which is what makes the average meaningful.
  </Accordion>
  <Accordion title="5. Your model's MAPE is 8% and the business is unhappy about persistent shortages. What do you suspect?">
  That MAPE is doing exactly what it is designed to do. Because over-prediction is penalised without bound and under-prediction is capped at 100%, the forecast that minimises MAPE sits well below the mean — measured in Layer 2 at 38.7% of the mean on a lognormal demand distribution. The model is not broken; the objective is asking it to under-forecast.
  </Accordion>
</AccordionGroup>

  



### The question this page leaves open

You now have a metric that fits the job. But you compute it on *some* data — and if you compute
it on the data the model trained on, you already know it will lie.

So which data? Split once? Split five ways? What if your data is a time series, or has multiple
rows per customer? Getting this wrong invalidates every number, and it is
[Module 3 — Validation strategy](/curriculum/p3/week-19/3-validation-strategy).



{/* CHAIN:BEGIN */}
    <Info>
    **You do not have to stop here.** This chapter goes two levels deeper — every
    link below jumps straight there:

    - [🔵 **Build**](#losses-and-their-minimisers) — [losses and their minimisers](#losses-and-their-minimisers) · [MAPE's trap, measured](#mapes-trap-measured) · [R-squared, honestly](#r-squared-honestly).
    - [🟣 **Edge**](#proper-scoring-rules-and-why-brier-and-log-loss-are-the-only-two-you-should-use) — [proper scoring rules, and why Brier and log-loss are the only two you should use](#proper-scoring-rules-and-why-brier-and-log-loss-are-the-only-two-you-should-use) · [recall@k, MRR and NDCG at depth — the bridge to Weeks 27–28](#recallk-mrr-and-ndcg-at-depth-the-bridge-to-weeks-2728) · [conformal prediction: intervals with a guarantee](#conformal-prediction-intervals-with-a-guarantee).
    - 🎯 **Prove it** — the concept, coding and interview questions this chapter
      produces are waiting in [Assessment](#assessment) at the bottom of this page.
    </Info>
{/* CHAIN:END */}
</Tab>

  <Tab title="🔵 Layer 2 — Build">

### Losses and their minimisers

The one fact that organises everything here: **the constant that minimises a loss tells you what that loss is asking your model to estimate.**

| Loss | Formula | Minimising constant | Choose it when… |
| --- | --- | --- | --- |
| Squared error | `mean((yhat - y)^2)` | the **mean** | you are summing predictions (total demand, total revenue) and large errors really are disproportionately bad |
| Absolute error | `mean(\|yhat - y\|)` | the **median** | the label is heavy-tailed and a few extreme rows should not dominate |
| Huber (delta) | quadratic within `delta`, linear beyond | between mean and median | you want MSE's smooth gradients with MAE's outlier resistance; `delta` is the crossover in label units |
| Pinball / quantile (q) | `mean(max(q*e, (q-1)*e))` | the **q-th quantile** | the cost of being under differs from the cost of being over, or you want an interval |
| MAPE | `mean(\|e\| / y)` | roughly a low quantile — **measured at the 38.7th percentile of the mean below** | a percentage genuinely is the unit of the business *and* `y` is bounded well away from zero |

The asymmetry knob is the useful one in practice. Retail stock-outs cost more than overstock; cloud capacity under-provisioning costs more than over-provisioning; an ETA that is late annoys more than one that is early. All of those are a pinball loss with `q` above 0.5, and scikit-learn gives it to you directly:

```python
from sklearn.ensemble import GradientBoostingRegressor
model = GradientBoostingRegressor(loss="quantile", alpha=0.90)   # alpha IS q
```

### MAPE's trap, measured

MAPE is the most-requested and least-defensible metric in business forecasting. Two defects, both demonstrable in five lines.

**It is asymmetric.** Under-predicting is bounded — you can never score worse than 100% by predicting zero — while over-predicting is unbounded. Truth 100, predict 50: APE 50%. Truth 100, predict 200: APE 100%. Truth 100, predict 500: APE 400%. Same *ratio* of error in the first pair, twice the penalty for the over-prediction.

**It blows up near zero.** Truth 1, predict 3: APE 200%, from an absolute error of 2. A single low-volume SKU can dominate a whole portfolio's MAPE.

The consequence, measured on 20,000 draws from a lognormal demand distribution (`sigma = 0.8`, `mu = 3.0`):

```python
import numpy as np
rng = np.random.default_rng(0)
d = rng.lognormal(3.0, 0.8, 20_000)
grid = np.linspace(d.min() + 0.01, np.quantile(d, 0.999), 4000)
best = min(grid, key=lambda c: np.mean(np.abs(c - d) / d))
print(f"mean {d.mean():.3f}  median {np.median(d):.3f}  MAPE-optimal constant {best:.3f}")
```

```
mean 27.640  median 19.991  MAPE-optimal constant 10.688
```

The MAPE-minimising forecast is **10.688 — 38.7% of the mean, and 53.5% of the median.** A team that optimises MAPE and ships the result will under-forecast demand by roughly 61% and then be surprised by stock-outs. This is not a subtle statistical effect; it is a two-thirds error caused entirely by the choice of metric.

If a business genuinely thinks in percentages, use **weighted MAPE** (`sum|e| / sum|y|`, which is scale-aware and does not blow up on small rows) or **symmetric MAPE** with its own known problems, or best of all report MAE alongside the mean of `y` so the reader can compute a percentage themselves. Never optimise MAPE.

### R-squared, honestly

R-squared is `1 - SS_res / SS_tot`: the fraction of the label's variance the model explains. It cannot decrease when you add a feature to a training fit, including a column of pure noise. Measured on California housing, 16,512 training rows, adding `k` columns of standard normal noise:

| Noise features added | Train R² | Adjusted train R² | **Test R²** |
| --- | --- | --- | --- |
| 0 | 0.61255 | 0.61236 | **0.57579** |
| 5 | 0.61280 | 0.61249 | 0.57497 |
| 50 | 0.61369 | 0.61233 | 0.57411 |
| 200 | 0.61736 | 0.61248 | 0.57168 |
| 1,000 | **0.63702** | 0.61342 | **0.55377** |

A thousand columns of literal random numbers bought **+0.0245 training R-squared**. Adjusted R-squared, `1 - (1-R²)(n-1)/(n-p-1)`, stayed within 0.001 of its starting value across the whole sweep — it charges you for each parameter and correctly reports "nothing happened". Test R-squared fell by 0.0220, which is the truth.

Rules: report **adjusted R-squared on training data** when comparing models with different feature counts, plain R-squared on held-out data, and never compare R-squared across datasets — an R² of 0.3 can be excellent for human behaviour and 0.95 can mean you leaked.

### Ranking metrics: the production implementation

This is the code Weeks 27 and 28 use, verbatim. Learn it here.

```python
# pip install "numpy==2.5.2"
import numpy as np

def recall_at_k(retrieved: list[int], relevant: set[int], k: int) -> float:
    """Fraction of relevant items that appear anywhere in the top k. Order-blind above k."""
    return len(set(retrieved[:k]) & relevant) / len(relevant)

def precision_at_k(retrieved: list[int], relevant: set[int], k: int) -> float:
    """Fraction of the k shown that were relevant. The denominator is k, not len(relevant)."""
    return len(set(retrieved[:k]) & relevant) / k

def mrr(retrieved: list[int], relevant: set[int]) -> float:
    """Reciprocal rank of the FIRST relevant hit. Nothing after it matters."""
    for i, cid in enumerate(retrieved, start=1):
        if cid in relevant:
            return 1.0 / i
    return 0.0

def ndcg_at_k(retrieved: list[int], relevant: set[int], k: int) -> float:
    """Position-discounted, normalised by the best ranking possible for this query."""
    dcg = sum(1.0 / np.log2(i + 2) for i, cid in enumerate(retrieved[:k]) if cid in relevant)
    ideal = sum(1.0 / np.log2(i + 2) for i in range(min(len(relevant), k)))
    return dcg / ideal if ideal > 0 else 0.0
```

Four implementation details that cause real bugs:

- **`enumerate(retrieved[:k])` starts at zero, so the discount is `1/log2(i+2)`** and position 1 gets a discount of exactly 1.0. If you see `1/log2(i+1)` with a one-based index it is the same thing; mixing the two conventions silently changes every number.
- **`ideal` uses `min(len(relevant), k)`.** A query with 20 relevant documents evaluated at k=10 can only ever place 10, so the ideal DCG must be capped at 10 terms or NDCG can never reach 1.0 and your averages are meaningless.
- **`relevant` is a `set`.** If your retriever can return the same chunk twice — which happens after a hybrid fusion — a list-based intersection double-counts and Recall@k can exceed 1.0.
- **The `ideal > 0` guard.** A query with no relevant documents in your labelled set must be excluded from the average, not scored as zero. Scoring it zero drags every reported number down by the fraction of unlabelled queries, which is one of the more common ways a retrieval eval quietly lies.

| Choose Recall@k as primary when… | Choose NDCG@k when… | Choose MRR when… |
| --- | --- | --- |
| everything in the top k is consumed equally — a language model receives all k chunks in its prompt | ordering inside the top k changes the outcome, e.g. you have a re-ranker whose entire job is order | the consumer looks at exactly one result: a "jump to source" link, a single citation, a voice answer |
| `k` is fixed by a hard budget (context window, page size) | you have **graded** relevance (0–3), which NDCG uses properly and MRR ignores | you want a metric that is brutal about the first position and easy to explain |
| you want the metric that is easiest to explain to a non-specialist | you are comparing systems that all find the same items but order them differently — as A and B did above | latency budgets mean you will show fewer results over time |

### Calibration: does 0.8 mean 80%?

A model can rank perfectly and still produce numbers that mean nothing, because **ROC-AUC is invariant to any monotone transform of the scores** (Module 1, Layer 3). If anything downstream multiplies your probability by a pound value — expected loss, expected revenue, a bid, Module 1's cost-optimal threshold — then the number must be *calibrated*: among all cases scored 0.30, about 30% should be positive.

Two measurements do the job.

- **Brier score** — the mean squared error of the probability: `mean((p - y)^2)`. Lower is better; the baseline is a constant prediction of the prevalence, which scores `pi*(1-pi)`.
- **Reliability diagram** — bin the predictions, plot mean predicted against observed frequency per bin, and compare with the 45-degree line. **Expected calibration error (ECE)** is the support-weighted mean absolute gap.

Measured on the telco churn dataset (7,043 rows, 26.54% churn, `xgboost==2.1.4`), deliberately using an over-confident booster — `max_depth=8`, 600 trees, no subsampling, `reg_lambda=0`:

```
model                AUC      AP      Brier     log-loss   ECE
raw booster        0.8061  0.5935  0.17520     0.66588   0.1366
+ Platt (sigmoid)  0.8061  0.5935  0.15538     0.48087   0.0188
+ isotonic         0.8029  0.5845  0.15017     0.47606   0.0229
constant baseline     —       —    0.19486        —         —
```

Read three things. **AUC does not move under Platt scaling** (0.8061 either way) — it is a monotone transform, so the ranking is untouched and only the *meaning* of the numbers changed. **ECE fell 7.3×**, from 0.1366 to 0.0188. And the raw model's Brier of 0.17520 was only 10.1% better than predicting the constant 0.2654 — which is a much less flattering summary than "AUC 0.81".

The raw model's reliability curve shows the classic over-confidence:

```
predicted  0.013  0.142  0.251  0.346  0.449  0.554  0.648  0.755  0.849  0.964
observed   0.121  0.273  0.339  0.382  0.357  0.333  0.596  0.491  0.643  0.663
```

In the top bin the model says **96.4%** and reality says **66.3%**. Any expected-value calculation using that bin overstates by 45%.

```python
# pip install "xgboost==2.1.4" "scikit-learn==1.9.0"
from sklearn.calibration import CalibratedClassifierCV, calibration_curve
from sklearn.frozen import FrozenEstimator

# X_cal / y_cal is a THIRD split, never used for fitting the booster
cal = CalibratedClassifierCV(FrozenEstimator(model), method="sigmoid").fit(X_cal, y_cal)
p = cal.predict_proba(X_test)[:, 1]
frac_pos, mean_pred = calibration_curve(y_test, p, n_bins=10, strategy="uniform")
```

`FrozenEstimator` (scikit-learn 1.6+) is how you calibrate an already-fitted model without refitting it. Before 1.6 this was `cv="prefit"`, now deprecated. Without it, `CalibratedClassifierCV` refits the base estimator inside its own cross-validation, which is correct but retrains your booster five times.

<Warning>
**The result that matters most in this section is the one that is not in the table.** A properly tuned booster on the same data — `max_depth=3`, `learning_rate=0.05`, early stopping at 112 trees — scores **Brier 0.13424** with **no calibration at all**, beating both calibrated versions of the over-fitted model (0.15538 and 0.15017) and beating the raw over-fitted model by 23.4%. Calibration is a repair, not an upgrade. Fix the model first; calibrate what remains.
</Warning>

### Trade-offs: Platt versus isotonic

| Choose Platt (sigmoid) when… | Choose isotonic when… |
| --- | --- |
| the calibration set is small — under roughly 1,000 rows, or under ~100 positives | you have thousands of calibration rows and can afford a flexible fit |
| the miscalibration looks like a consistent squeeze or stretch (a sigmoid shape) | the distortion is not sigmoid-shaped — common after resampling (Week 20 Module 4) |
| you need the ranking preserved exactly — it is strictly monotone | a slight AUC loss is acceptable: measured **0.8061 → 0.8029** here, because isotonic's step function creates ties |
| you want two parameters and near-zero overfitting risk | you want the lowest achievable Brier: 0.15017 versus 0.15538 here |

### War story: the bid that was 45% too high

*A modelled composite — assembled from recurring patterns rather than one company, with illustrative figures. The mechanism is real and the measured tables elsewhere on this page are unaffected; the incident is not a case study. See [About the numbers](/start/about-the-numbers).*

An ad-tech team ran a click-prediction model whose output was multiplied directly by a conversion value to set a bid. The model's AUC was healthy and stable for months. Spend, however, ran consistently over plan, and the excess was concentrated in the highest-value inventory.

The cause was in the top reliability bin, exactly as in the table above. The model had been retrained with deeper trees during a "quality push", which raised AUC by 0.004 and destroyed calibration at the top of the range: predictions near 0.96 were converting at about 0.66. Because the bid was `p * value`, every bid in the most expensive inventory was inflated by roughly 45%, and because the auction is second-price the team still won those auctions — they just paid more for them. Nothing in the monitoring caught it: AUC was up, log-loss was not on the dashboard, and there was no reliability check.

The fix was fifteen lines: log the mean predicted probability and the realised conversion rate per decile every day, alert when the gap in any decile exceeds 0.05 absolute, and add a Platt layer fitted weekly on a held-out slice. The lasting lesson is the general one: **if a probability is multiplied by money, monitor calibration, not ranking.**

### Design checkpoint

> A demand-forecasting team reports 8.4% MAPE and wants to ship. Warehouses are complaining about stock-outs. What do you check, in what order?

<Accordion title="Model answer">
First, reproduce their metric and compute the **mean signed error**, not the absolute one. If the model is systematically under-forecasting, the signed mean will be clearly negative while MAPE says nothing — MAPE cannot show direction. Second, plot the MAPE-optimal constant against the mean of the label, exactly as measured above: on lognormal demand the MAPE minimiser sits at 38.7% of the mean, so an 8.4% MAPE is entirely consistent with a forecast biased 60% low on the heavy-tailed SKUs.

Then re-frame the objective around the actual asymmetry. A stock-out costs the margin on the lost sale plus a substitution risk; overstock costs holding cost plus markdown. Those are two different numbers, so the right loss is **pinball at a `q` derived from them** — `q = C_understock / (C_understock + C_overstock)`, which is the newsvendor formula and structurally the same derivation as Module 1's cost threshold. Fit `GradientBoostingRegressor(loss="quantile", alpha=q)` and report weighted MAPE plus mean signed error plus achieved service level, and never report plain MAPE again. Finally, check the low-volume SKUs separately: they dominate MAPE and may be better served by a reorder-point rule than a model at all.
</Accordion>

  




{/* CHAIN:BEGIN */}
    <Info>
    **One level further down.** [🟣 **Edge**](#proper-scoring-rules-and-why-brier-and-log-loss-are-the-only-two-you-should-use) holds [proper scoring rules, and why Brier and log-loss are the only two you should use](#proper-scoring-rules-and-why-brier-and-log-loss-are-the-only-two-you-should-use) · [recall@k, MRR and NDCG at depth — the bridge to Weeks 27–28](#recallk-mrr-and-ndcg-at-depth-the-bridge-to-weeks-2728) · [conformal prediction: intervals with a guarantee](#conformal-prediction-intervals-with-a-guarantee) — and the senior
    interview question in [Assessment](#assessment) assumes you have read it.
    </Info>
{/* CHAIN:END */}
</Tab>

  <Tab title="🟣 Layer 3 — Edge">

### Proper scoring rules, and why Brier and log-loss are the only two you should use

A **scoring rule** takes a predicted distribution and an outcome and returns a penalty. It is **proper** if the forecaster's expected penalty is minimised by reporting their true belief, and **strictly proper** if that minimum is unique. This is not a technicality — an improper scoring rule pays your model to lie.

Squared error on probabilities (Brier) and negative log-likelihood (log-loss) are both strictly proper. Accuracy is not: if you believe the true probability is 0.6, accuracy is maximised by reporting 1.0, so a model tuned on accuracy is being paid to be over-confident. F1 is not proper either. This is the formal reason that the Module 1 metrics cannot substitute for the ones here.

Brier decomposes (Murphy, 1973) into three interpretable terms:

```
Brier  =  reliability  -  resolution  +  uncertainty
```

- **uncertainty** is `pi*(1-pi)`, a property of the data alone — 0.19486 on telco churn. Nothing you do changes it.
- **reliability** is the calibration error, weighted by bin size. Lower is better. This is the term Platt scaling attacks.
- **resolution** is how far your predictions spread away from the base rate. Higher is better. This is the term more data and better features attack.

Run the numbers from Layer 2 through that lens. Platt scaling took Brier from 0.17520 to 0.15538 — a **reliability** improvement, with AUC unchanged at 0.8061 confirming resolution did not move. Tuning the model took Brier to 0.13424 with AUC rising to 0.8498 — a **resolution** improvement. The decomposition tells you which of the two levers is available, and it is the reason "just calibrate it" is bad advice for a model that is genuinely weak.

The choice between Brier and log-loss is about how you want confident mistakes punished. Log-loss is unbounded: a single confident, wrong prediction at `p = 0.999` contributes 6.9 to the sum, while Brier contributes at most 1.0. **Use log-loss when a confident error is catastrophic (medical, safety) and Brier when it is merely expensive**, and clip probabilities away from 0 and 1 before computing log-loss or one row will define your metric.

### Recall@k, MRR and NDCG at depth — the bridge to Weeks 27–28

These three are the entire scoreboard for the retrieval half of this course, so it is worth knowing precisely what each is blind to.

**Recall@k is blind to order and to everything below k.** Both properties are deliberate and both bite. A retriever that puts the answer at rank 10 out of 10 scores the same 1.0 as one that puts it at rank 1 — which is correct if all ten chunks go into a prompt, and badly wrong if a re-ranker will only forward the top three. Week 27 Module 5 makes Recall@10 the primary metric for exactly this reason and then switches to NDCG the moment a re-ranker appears in Week 28 Module 4.

**MRR discards every hit after the first.** For a query with five relevant documents ranked 1, 2, 3, 4, 5, MRR is 1.0 — the same as for a query with one relevant document at rank 1. It is the right metric when the consumer reads one result and the wrong one whenever recall matters.

**NDCG's discount is a modelling choice, not a law.** The `1/log2(i+2)` curve is conventional, from Järvelin and Kekäläinen (2002), chosen because it decays slowly enough to keep the tail relevant. It is *not* a measured model of user attention: real click-through decays faster than logarithmically at the top of a search results page. If your product has measured position bias, substitute it — the metric is still normalised and still comparable across queries, and it now matches your users. Two things you must fix and document: the discount function and whether `ideal` is capped at k.

There is a fourth number that Weeks 27 and 28 need and that this module cannot give you: **the size of the labelled eval set determines whether a difference is real.** A change from 0.84 to 0.87 Recall@10 on 100 queries is roughly 3 queries, and a paired bootstrap over those 100 queries will show an interval that comfortably contains zero. Module 5 supplies the machinery; Week 27's ablation table applies it and reports three of its six rows as not significant.

### Conformal prediction: intervals with a guarantee

Quantile regression gives an interval by fitting a model per quantile. It is easy and it does **not** come with a coverage guarantee. Measured on California housing, `GradientBoostingRegressor(loss="quantile")` at alpha 0.05 and 0.95, 200 estimators, 4,128 test rows:

```
quantile GBM 5–95 interval:  coverage 0.8653  (target 0.90)   mean width 1.7422
```

It **under-covers by 3.5 points**. Nothing is broken — the quantile estimates are themselves fitted quantities with error, and their errors do not cancel.

**Split conformal prediction** fixes this with a guarantee that requires only exchangeability — no distributional assumption, no assumption that the model is any good:

```python
# pip install "numpy==2.5.2" "scikit-learn==1.9.0"
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestRegressor

X_fit, X_cal, y_fit, y_cal = train_test_split(X_train, y_train, test_size=0.25, random_state=1)
model = RandomForestRegressor(n_estimators=200, min_samples_leaf=5,
                              random_state=0, n_jobs=-1).fit(X_fit, y_fit)

residuals = np.abs(y_cal - model.predict(X_cal))          # conformity scores
n = len(residuals)
level = np.ceil((n + 1) * 0.90) / n                       # the finite-sample correction
q = np.quantile(residuals, level, method="higher")

pred = model.predict(X_test)
lower, upper = pred - q, pred + q
print(f"q={q:.4f}  coverage {((y_test >= lower) & (y_test <= upper)).mean():.4f}  width {2*q:.4f}")
```

```
q=0.7587  coverage 0.8886 (target 0.90)  width 1.5174
```

**0.8886 against a target of 0.90**, and *narrower* than the quantile-regression interval (1.5174 versus 1.7422). The guarantee is marginal coverage of at least `1 - alpha` in expectation over the calibration draw, and the `ceil((n+1)*(1-alpha))/n` term is exactly the finite-sample correction that makes it hold — using a plain `np.quantile(residuals, 0.90)` loses the guarantee, by a little at large `n` and by a lot at small `n`.

The honest limitation, and it is a big one: split conformal with absolute residuals produces a **constant-width** interval. Every prediction gets ±0.7587 whether the model is confident or not. Real uncertainty is heteroscedastic, and the fix is a normalised conformity score, `|y - yhat| / sigma(x)`, where `sigma(x)` is a second model fitted to predict the residual magnitude — this is **conformalised quantile regression** (Romano, Patterson and Candès, 2019), which keeps the coverage guarantee *and* varies the width.

### Failure modes

| Failure | Signature | Detector |
| --- | --- | --- |
| MAPE optimised, forecast biased low | persistent stock-outs while MAPE looks fine | report **mean signed error** beside MAPE; it will be clearly negative |
| MAPE infinite or enormous | a `y` at or near zero | assert `y.min() > 0` before computing; switch to weighted MAPE |
| R-squared climbing with every feature | R² cannot fall on training data | adjusted R² on train, plain R² on held-out; measured +0.0245 from pure noise |
| NDCG can never reach 1.0 | `ideal` not capped at k when a query has more than k relevant items | assert `ndcg_at_k(ideal_ranking, relevant, k) == 1.0` in a unit test |
| Recall@k above 1.0 | duplicate IDs in the retrieved list after fusion | intersect **sets**, and assert `len(set(retrieved)) == len(retrieved)` |
| Retrieval metrics drifting down over releases | unlabelled queries scored as 0 instead of excluded | count and report the excluded queries as a separate number |
| Expected-value calculations consistently wrong in one direction | miscalibration, invisible to AUC | per-decile predicted-versus-observed, alerting at 0.05 absolute gap |
| Calibration "fixed" but ranking got worse | isotonic's step function ties scores together | measured AUC 0.8061 → 0.8029; use Platt when the ranking is the product |
| Conformal interval under-covers badly | plain `np.quantile` without the `(n+1)/n` correction, or a calibration set reused for fitting | assert the calibration split is disjoint; use `method="higher"` |

### Papers

- **Järvelin, K. & Kekäläinen, J. (2002), "Cumulated Gain-Based Evaluation of IR Techniques," *ACM TOIS* 20(4).** *Takeaway:* the log discount and the normalisation by ideal DCG were introduced to make graded relevance comparable across queries of different difficulty — everything in Week 27's retrieval scoreboard descends from Section 4 of this paper.
- **Gneiting, T. & Raftery, A. E. (2007), "Strictly Proper Scoring Rules, Prediction, and Estimation," *JASA* 102(477).** *Takeaway:* only a strictly proper rule such as Brier or log-loss is uniquely minimised by reporting your true belief, which is the formal reason accuracy and F1 cannot be used to grade probabilities.
- **Murphy, A. H. (1973), "A New Vector Partition of the Probability Score," *Journal of Applied Meteorology* 12(4).** *Takeaway:* Brier decomposes into reliability minus resolution plus uncertainty, which tells you whether your model needs calibrating or needs to be better — the distinction that made the 0.15538-versus-0.13424 result above interpretable.
- **Niculescu-Mizil, A. & Caruana, R. (2005), "Predicting Good Probabilities with Supervised Learning," *ICML*.** *Takeaway:* boosted trees and SVMs are systematically over-confident in a sigmoid-shaped way while bagged trees are nearly calibrated, and Platt scaling wins on small calibration sets while isotonic wins above roughly 1,000 examples.
- **Romano, Y., Patterson, E. & Candès, E. (2019), "Conformalized Quantile Regression," *NeurIPS*.** *Takeaway:* combining conformal calibration with quantile regression keeps the finite-sample coverage guarantee while letting the interval width vary with the input, which fixes the constant-width limitation demonstrated above.

### Stretch challenge

Build the **metric-choice audit** for a forecasting or ranking system you can get real data for, and make it produce a decision rather than a table.

For a regression target: fit the same model family five times under squared error, absolute error, Huber, pinball at `q = 0.7` and pinball at `q = 0.9`. Report all five models under all five metrics as a 5×5 matrix, plus mean signed error, plus the achieved service level if there is an asymmetric cost. Then the open question: **the matrix's diagonal is not always the winner — find a cell where a model trained on one loss beats the model trained on the metric being reported, and explain the mechanism.** (It happens; it is usually about outliers in the validation set rather than about the model.)

For a ranking target: take any labelled query set, compute Recall@k for k in 1, 3, 5, 10, 20 plus MRR plus NDCG@10, and then construct **two systems that tie on your chosen primary metric but differ by more than 0.15 on another one** — the Layer 1 example is the template. Present both to someone who will use the system and record which they prefer. That preference, not the metric, is what you should have been optimising, and the deliverable is the paragraph explaining which metric you will make primary in Week 27 as a result.

  




{/* CHAIN:BEGIN */}
    ### The floor of this topic

    You have read as deep as this course goes here — the story, the
    machinery, and the scars. Two ways out: prove it against the senior questions in
    [Assessment](#assessment) below, or carry on to [Validation strategy](/curriculum/p3/week-19/3-validation-strategy), where the story continues at every depth.
{/* CHAIN:END */}
</Tab>
</Tabs>

## Hands-on

<Card title="Do this now" icon="hammer" horizontal>
Build a reliability diagram for an XGBoost model, then calibrate it and show the improvement in Brier score — and then show that fixing the model beats calibrating it.
</Card>

<Steps>
  <Step title="Three splits, not two">
  Telco churn, 7,043 rows. Split 75/25 train/test, then split the training half again 70/30 into fit and calibration. The calibration set must never have been seen by the booster. Assert this in code.
  </Step>
  <Step title="Break it on purpose">
  Fit `XGBClassifier(n_estimators=600, max_depth=8, learning_rate=0.1, subsample=1.0, reg_lambda=0.0)`. Report AUC, average precision, Brier and log-loss. You should land near AUC 0.806, Brier 0.1752.
  </Step>
  <Step title="Measure the miscalibration">
  Reliability curve with 10 uniform bins, plus ECE computed as the support-weighted mean absolute gap. Reproduce the top-bin result: predicted 0.964, observed 0.663. Plot it with the 45-degree line.
  </Step>
  <Step title="Fix it two ways">
  `CalibratedClassifierCV(FrozenEstimator(model), method=...)` for both `"sigmoid"` and `"isotonic"`. Report Brier, log-loss, ECE and AUC for each. Note which one moved AUC and say why.
  </Step>
  <Step title="Then fix the model">
  Refit with `max_depth=3`, `learning_rate=0.05`, `early_stopping_rounds=50` on the calibration set as `eval_set`. Report its Brier with no calibration. Write the paragraph comparing it with the two calibrated versions — this is the deliverable.
  </Step>
  <Step title="Ranking metrics, tested">
  Implement all four ranking functions and write unit tests: NDCG of the ideal ranking is exactly 1.0; Recall@k never exceeds 1.0 given duplicates; a query with no relevant items raises or is excluded rather than scoring 0.
  </Step>
</Steps>

Time box: **4 hours.**

## The whole idea on one whiteboard

Every metric is a bet about which errors you are willing to trade for which. Squaring the error says a single 10-unit miss is worse than ten 1-unit misses, so its minimiser is the **mean**. Taking absolute value says they are equal, so its minimiser is the **median**. Tilting the absolute value says being under is worse than being over, so its minimiser is a **quantile**. Ranking metrics make the same kind of bet about *positions* rather than magnitudes: Recall@k does not care where in the top k an item landed, NDCG does. And a probabilistic metric asks a different question entirely — not "was the ranking right" but "does 0.8 mean 80%" — which no amount of AUC can answer.

## The whiteboard

T2 build-up, four frames: (1) the error axis with the V and the parabola drawn over it, and the two minimisers written in; (2) the outlier row landing and the ×69 / ×1.00 annotations appearing; (3) the two ranked lists with identical Recall@5 circled in green and the MRR/NDCG gap in red; (4) the reliability diagram with the sagging raw curve, then the Platt curve snapping onto the diagonal.

<Tip>
**Draw it yourself.** Reproduce the sketch above from memory on paper, then compare.
This is graded — it is the cheapest retention exercise in the course.
[Blank version](/assets/diagrams/p3-w19-m2-blank.svg) · [Editable source](/assets/diagrams/p3-w19-m2.excalidraw)
</Tip>

## Apply it at work

<AccordionGroup>
  <Accordion title="If you're a student" icon="graduation-cap">
  **Resume line:** "Diagnosed and corrected probability miscalibration in a gradient-boosted classifier: expected calibration error 0.137 → 0.019 via Platt scaling, and showed that retuning the model rather than calibrating it reduced Brier score a further 13.5%."

  **Interview move:** when asked about regression metrics, answer with minimisers. "MSE's minimiser is the conditional mean, MAE's is the conditional median, pinball's is a quantile — so the question is which of those the business is paying for." Then offer the MAPE result: "and I would not use MAPE at all; on lognormal demand its optimum sits at 38.7% of the mean."

  **Portfolio:** the ranking-metric notebook is worth more than it looks, because you can point at it in Week 27 and say "this is the eval harness I built in Week 19 and reused." Interviewers like reuse.
  </Accordion>
  <Accordion title="If you're working" icon="briefcase">
  1. **Add a per-decile calibration monitor** to any model whose output is multiplied by money. Log mean predicted versus realised rate per decile daily; alert on a gap above 0.05 absolute. Fifteen lines, and it is exactly the check that would have caught the 45% bid inflation in the war story.
  2. **Find every MAPE on a dashboard you own** and put mean signed error next to it. If the signed error is consistently negative, you have a metric-induced bias, not a model problem, and the fix is a pinball loss at `q = C_under/(C_under + C_over)`.
  3. **Wrap your next point forecast in a split-conformal interval.** Twelve lines, no retraining, and a coverage guarantee you can state in a review. Measured above at 0.8886 coverage against a 0.90 target, narrower than the quantile-regression alternative.
  </Accordion>
</AccordionGroup>

## Common mistakes

| Symptom | Cause | Fix |
| --- | --- | --- |
| Forecast consistently short, MAPE looks fine | MAPE's minimiser sits well below the mean — measured at 38.7% of it | report mean signed error; optimise pinball loss at the cost-derived `q` |
| MAPE is `inf` or in the thousands | a label at or near zero | assert `y.min() > 0`; use weighted MAPE (`sum\|e\|/sum\|y\|`) |
| RMSE and MAE tell different stories | a few large errors; RMSE/MAE ratio is a free outlier detector | report both plus the 95th percentile of absolute error |
| Training R-squared always improves | R² cannot fall on training data, even with noise features | adjusted R² on train; held-out R² for decisions |
| NDCG never reaches 1.0 | `ideal` not capped at `k` for queries with more than `k` relevant items | `min(len(relevant), k)`; unit-test the ideal ranking |
| Recall@k above 1.0 | duplicate IDs in the retrieved list | intersect sets and assert the retrieved list has no duplicates |
| Retrieval scores drift downward each release | queries with no labels scored 0 rather than excluded | exclude them and report the count separately |
| AUC good, expected-value maths wrong | AUC cannot see miscalibration; it is invariant to monotone transforms | Brier + reliability diagram + per-decile monitor |
| Calibration helped Brier but hurt AUC | isotonic creates ties by construction — measured 0.8061 → 0.8029 | Platt when ranking is the product, isotonic when the probability is |
| Calibration barely helped | the model is weak, not miscalibrated | check Brier's decomposition: you need resolution, not reliability. Tuning got 0.1752 → 0.1342 |
| Conformal interval under-covers | plain quantile without `ceil((n+1)*(1-a))/n`, or a reused calibration split | use the correction and `method="higher"`; assert disjoint splits |

## Assessment

**Concept questions (8).**
1. Give the minimising constant for squared error, absolute error and pinball loss at `q = 0.9`.
2. One prediction out of ten goes badly wrong. By roughly what factor does MSE move, and by what factor does median absolute error move?
3. Why is MAPE asymmetric, and in which direction does it bias a forecast?
4. Write `ndcg_at_k` from memory, including the discount and the normalisation.
5. Two systems have identical Recall@5. Give a concrete case where one is clearly better, and name the two metrics that show it.
6. What is a strictly proper scoring rule, and why is accuracy not one?
7. Brier decomposes into three terms. Which term does Platt scaling improve, and which does better feature engineering improve?
8. Why does split conformal use `ceil((n+1)*(1-alpha))/n` rather than `1-alpha`?

**Coding challenge.** Implement `evaluate_ranking(runs, qrels, ks)` returning Recall@k, Precision@k, MRR and NDCG@k averaged over queries, plus a paired bootstrap 95% interval on the difference between two runs. Hidden tests check: NDCG of the ideal ranking is exactly 1.0 for every k; queries absent from `qrels` are excluded from the average, not scored zero; duplicate document IDs never push Recall above 1.0; and the whole thing runs on 10,000 queries × 100 results in under 5 seconds.

**Interview questions.**
- *Junior:* "When would you use MAE instead of RMSE?"
- *Mid:* "Our recommender's Recall@10 went up and users are complaining more. How is that possible?"
- *Senior:* "A downstream system multiplies our model's probability by a pound value. What do you add to the evaluation, and what do you monitor in production?"

**Draw from memory.** The two ranked lists with identical Recall@5 and divergent MRR/NDCG, plus the reliability diagram with the sagging raw curve and the corrected one.

## Sources & further reading

**Primary docs**
- [scikit-learn 1.9 — regression metrics](https://scikit-learn.org/stable/modules/model_evaluation.html#regression-metrics) — read this for the exact definitions of `mean_pinball_loss`, `d2_pinball_score` and why `mean_absolute_percentage_error` is documented with an explicit warning about small denominators.
- [scikit-learn 1.9 — probability calibration](https://scikit-learn.org/stable/modules/calibration.html) — read this for the `FrozenEstimator` pattern that replaced `cv="prefit"`, and for the figure showing which model families are over- and under-confident.
- [XGBoost 2.1 — learning task parameters](https://xgboost.readthedocs.io/en/release_2.1/parameter.html#learning-task-parameters) — read this for `objective="reg:quantileerror"` and `quantile_alpha`, which give you pinball loss natively rather than through scikit-learn's slower gradient booster.

**Papers**
- [Järvelin, K. & Kekäläinen, J. (2002), "Cumulated Gain-Based Evaluation of IR Techniques," *ACM TOIS* 20(4).](https://doi.org/10.1145/582415.582418) *Takeaway:* the log discount plus normalisation by the ideal ranking is what makes graded relevance comparable across queries, and it is the definition Weeks 27 and 28 use unchanged.
- [Gneiting, T. & Raftery, A. E. (2007), "Strictly Proper Scoring Rules, Prediction, and Estimation," *JASA* 102(477).](https://doi.org/10.1198/016214506000001437) *Takeaway:* only a strictly proper rule is uniquely optimised by honest reporting, which is why probabilities must be graded with Brier or log-loss and never with accuracy or F1.
- [Murphy, A. H. (1973), "A New Vector Partition of the Probability Score," *Journal of Applied Meteorology* 12(4). *Takeaway:* reliability minus resolution plus uncertainty](https://doi.org/10.1175/1520-0450(1973)012%3C0595:ANVPOT%3E2.0.CO;2) — the decomposition that tells you whether to calibrate the model or replace it.
- [Niculescu-Mizil, A. & Caruana, R. (2005), "Predicting Good Probabilities with Supervised Learning," *ICML*.](https://doi.org/10.1145/1102351.1102430) *Takeaway:* boosting is reliably over-confident in a sigmoid-shaped way, so Platt scaling is the right first attempt, and isotonic only pays off above roughly 1,000 calibration examples.
- [Romano, Y., Patterson, E. & Candès, E. (2019), "Conformalized Quantile Regression," *NeurIPS*.](https://arxiv.org/abs/1905.03222) *Takeaway:* conformal calibration on top of quantile regression retains the distribution-free coverage guarantee while letting interval width adapt to the input, fixing split conformal's constant width.

**Go deeper**
- [Angelopoulos, A. N. & Bates, S. (2023), *A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification*, sections 1 to 3.](https://arxiv.org/abs/2107.07511) Forty pages, worked code, and the clearest available explanation of why the `(n+1)/n` correction is exactly what makes the guarantee finite-sample rather than asymptotic.

<Card title="Solutions & answer key" icon="lock" href="/solutions/p3/week-19/p3-w19-m2">
  Attempt the lab first. Seriously.
</Card>

{/* --- hand-written body below this line is preserved on regeneration --- */}
