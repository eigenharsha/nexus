---
title: "Week 18 — Trees, Ensembles & Unsupervised Learning"
description: "learner can win most tabular problems with gradient boosting and knows why it wins; can segment data without labels."
icon: "calendar"
---
{/* AUTHORED */}

## Outcome

<Note>By Sunday you will be able to win most tabular problems with gradient boosting and explain *why* it wins, and to segment data without labels while knowing exactly which claims that segmentation supports.</Note>

## The through-line

**M1** builds one tree, computes a Gini split by hand, and finds its two structural weaknesses: greedy search, and axis-alignment — measured as a depth-3 tree going from 0.9105 to a perfect 1.0000 on the same data after a 45-degree rotation. It also measures the instability that motivates everything else: flip **one** training label in 398 and 3.78% of test predictions change.

**M2** fixes that instability by averaging. A 200-tree forest cuts the same perturbation's effect to 0.37%, a **10.2x** reduction, and the variance formula `rho*sigma^2 + (1-rho)*sigma^2/B` explains both why it works and why it stops. It also demolishes `feature_importances_`: a column of pure random noise takes **81.8%** of the impurity importance, and deleting it raises held-out accuracy by **7.9 points**.

**M3** combines trees the other way — in sequence, each fixing the last one's residual — and derives XGBoost's second-order objective properly, so `w* = -G/(H + lambda)` is something you can write from memory. A 31-configuration study then shows that `max_depth` moves test AUC **17x more** than `learning_rate` once early stopping is on.

**M4** removes the labels. K-Means is two trivial steps, and every one of its failures is predictable from one fact: it is a Gaussian mixture with the covariance frozen spherical. **M5** removes the dimensions, and shows exactly which claims a t-SNE or UMAP plot cannot support — measured, on constructed data where the truth is known by design.

Two forward links matter. Week 27's IVF vector index is M4's K-Means run on embeddings, and Week 27's retrieval-quality inspection is M5's workflow. Neither is an analogy.

## Modules

<CardGroup cols={2}>
  <Card title="1 · Decision trees" icon="pen-line" href="/curriculum/p3/week-18/1-decision-trees">Gini by hand, CART from scratch, and the two things trees are structurally bad at.</Card>
  <Card title="2 · Bagging & random forests" icon="pen-line" href="/curriculum/p3/week-18/2-bagging-random-forests">The bootstrap, the variance formula, OOB scoring, and the feature-importance trap with a number on it.</Card>
  <Card title="3 · Gradient boosting & XGBoost" icon="pen-line" href="/curriculum/p3/week-18/3-gradient-boosting-xgboost">Newton steps, the split-gain formula, a 31-config sensitivity study, and SHAP at 0.65 ms/row.</Card>
  <Card title="4 · Unsupervised learning: clustering" icon="pen-line" href="/curriculum/p3/week-18/4-unsupervised-learning-clustering">Lloyd's algorithm, choosing k three ways, and the three shapes K-Means gets wrong.</Card>
  <Card title="5 · Dimensionality reduction & representation" icon="pen-line" href="/curriculum/p3/week-18/5-dimensionality-reduction-representation">PCA as a tool, t-SNE as a view, and the constructed counterexample that proves the difference.</Card>
</CardGroup>

<Card title="Lab — Customer churn, end to end" icon="flask-conical" href="/curriculum/p3/week-18/lab">
  Baseline to logistic regression to random forest to XGBoost, with a model card and a stakeholder one-pager. Three tracks.
</Card>

## The six numbers to leave the week with

| Number | What it is | Where |
| --- | --- | --- |
| **0.9105 to 1.0000** | a depth-3 tree on the same data before and after a 45-degree rotation — the cost of axis-alignment | M1 |
| **3.78% to 0.37%** | fraction of test predictions changed by flipping one training label: one tree versus 200 | M1, M2 |
| **0.8182 / +7.9 points** | share of impurity importance taken by a random ID column, and the accuracy gained by deleting it | M2 |
| **17x** | how much more `max_depth` moves test AUC than `learning_rate` under early stopping | M3 |
| **767x** | TreeSHAP versus KernelSHAP, per row (0.65 ms against 501 ms) | M3 |
| **6.63 rendered as 1.09** | a true inter-cluster distance ratio, as drawn by t-SNE at default perplexity | M5 |

## Prerequisites check

You should already be able to: state what overfitting looks like in a train/validation gap (Week 17 Module 4); derive the gradient of log-loss (Week 17 Module 3); build a leak-free `Pipeline` (Week 17 Module 5); and compute a Euclidean distance and an eigenvector (Week 11). Week 18 assumes all four without re-teaching them.

## Environment

```bash
uv venv --python 3.12 .venv && source .venv/bin/activate
uv pip install "numpy==2.5.2" "scikit-learn==1.9.0" "scipy==1.18.1" "pandas==3.0.5" \
               "xgboost==2.1.4" "lightgbm==4.7.0" "shap==0.52.0" "umap-learn==0.5.12" \
               "matplotlib==3.11.1" "pytest==9.1.1"
```

On macOS, XGBoost and LightGBM need the OpenMP runtime: `brew install libomp`. Without it you get `XGBoostError: Library (libxgboost.dylib) could not be loaded` and nothing else works.

Every number in this week's pages was **measured** on an Apple M3 Pro (11 cores, 18 GB) with exactly these versions and Python 3.12.9 — the machine this course was written on. Absolute timings will differ on your machine; the ratios should not. The Gini arithmetic, the bagging variance formula and the `num_leaves` / `max_depth` relationship are **derived** and hold anywhere. See [About the numbers](/start/about-the-numbers).

## This week's rhythm

| Day | Student (12–15 h) | Working professional (5–7 h) |
| --- | --- | --- |
| Mon–Tue | M1 + M2, layers 1 and 2 | M2 layer 2, M3 layers 1 and 2 |
| Wed–Thu | Lab `basic` → `standard` | Lab `standard` |
| Fri | M3 all three layers — it is the week's centre of gravity | M3 layer 3, the sensitivity study |
| Sat | M4 + M5, then lab `hard`, push to GitHub | M4/M5 layer 2, lab `hard` (optional) |
| Sun | Assessment + flashcards + draw-from-memory | Assessment |

## Deliverables

- A churn repository with the full progression: baseline, logistic regression, random forest, XGBoost, each with its number
- A **model card** — this is the artifact most likely to be reused directly at work
- A stakeholder one-pager with an estimated monetary impact
- The 31-configuration sensitivity table with the seed-noise floor marked on it
- The t-SNE counterexample figure with the size and distance ratios
- A customer segmentation that passes the single-feature-dominance and cross-seed stability tests

{/* --- hand-written body below this line is preserved on regeneration --- */}
