---
title: "Week 26 — Quantization, PEFT & Local Serving"
description: "learner fine-tunes a small model on their own hardware and serves it — with the memory arithmetic fully understood."
icon: "calendar"
---

{/* AUTHORED */}

## Outcome

<Note>
By Sunday you will have quantized a model and measured what it cost you, decided — with evidence —
whether to fine-tune at all, run a QLoRA fine-tune on a single 24 GB GPU, forced its output to
conform to a schema, and served it behind an OpenAI-compatible endpoint you capacity-planned
yourself.
</Note>

Week 25 opened the model. This week you *operate* it on hardware you can afford, and every step is
an arithmetic problem before it is an engineering one.

## The through-line

1. **Precision & quantization.** The same weights in fewer bits. Smaller means faster, because
   Week 25 M5 showed decode is bandwidth-bound. What does it cost in quality, and can your eval
   even detect the difference?
2. **Fine-tuning: when and why.** The decision, before the mechanism. Full fine-tuning an 8B model
   needs 132 GB. Also: fine-tuning is the wrong tool for facts, and here is the measurement.
3. **LoRA & QLoRA.** Week 11's SVD, cashed in. 132 GB becomes 14.2 GB, and a 4096×4096 matrix gets
   a 65,536-parameter patch.
4. **Structured output.** Making the model's output parseable by construction rather than by luck —
   and understanding precisely what that does and does not fix.
5. **Local & self-hosted serving.** Week 9's virtual memory, applied to the KV cache. Then the
   arithmetic that says whether you should be doing any of this instead of calling an API.

## Modules

<CardGroup cols={2}>
  <Card title="1 · Numeric precision & quantization" icon="pen-line" href="/curriculum/p4/week-26/1-numeric-precision-quantization">Bit layouts, NF4, outlier features, and a Pareto curve with honest confidence intervals.</Card>
  <Card title="2 · Fine-tuning: when and why" icon="pen-line" href="/curriculum/p4/week-26/2-fine-tuning-when-and-why">The decision table, the 16-bytes-per-parameter arithmetic, and why facts belong in the prompt.</Card>
  <Card title="3 · LoRA & QLoRA" icon="pen-line" href="/curriculum/p4/week-26/3-lora-qlora">Week 11's SVD applied prospectively; the rank sweep; multi-adapter serving economics.</Card>
  <Card title="4 · Structured output & tool-calling" icon="pen-line" href="/curriculum/p4/week-26/4-structured-output-tool-calling-behaviour">Constrained decoding guarantees syntax and nothing else. Measure both.</Card>
  <Card title="5 · Local & self-hosted serving" icon="pen-line" href="/curriculum/p4/week-26/5-local-self-hosted-serving">Ollama, vLLM, PagedAttention, and the self-host break-even volume.</Card>
  <Card title="Lab · QLoRA for strict structured output" icon="flask-conical" href="/curriculum/p4/week-26/lab">Three tracks, ending in a served endpoint and a cost analysis.</Card>
</CardGroup>

## The five numbers you should leave with

| Number | What it is |
| --- | --- |
| `16 bytes/parameter` → 132.6 GB for 8B | Full fine-tuning memory. It is why LoRA exists. |
| `4096×4096 = 16.8M` vs `2×4096×8 = 65,536` | LoRA rank-8 on one matrix: 256× fewer trainable parameters. |
| `n = 50` → 95% CI ≈ ±13 points | Why most quantization comparisons you read online prove nothing. |
| Validity 81.5% → 100%, F1 0.71 → 0.74 | Constrained decoding fixes syntax, not semantics. |
| `1.22B output tokens/month`, 37% utilisation | Self-hosting break-even against a $0.60/M hosted model on one A10G. |

## Hardware and cost

<Note>
**Where this week's numbers come from.** The memory arithmetic (16 bytes/parameter, LoRA rank
counts, QLoRA totals) is **derived** and exact. Ollama figures on Apple Silicon are **measured**.
Every A10G, A100 and vLLM throughput figure is a **reference figure** — modelled from published
benchmarks and vendor specs, because the authors had no GPU. The timings in the paths below are
planning estimates of the same kind. Trust the ratios; measure your own absolutes. See [About the numbers](/start/about-the-numbers).
</Note>

<Info>
**Everything this week runs on a single 24 GB GPU for under $6 of rental**, or free on a Colab T4
with the documented fallbacks. The Layer 1 material and Module 5's Ollama section run on a laptop
with 16 GB of RAM and no GPU at all. Total cloud spend if you rent an A10G for the QLoRA run and
the serving benchmarks: about 5.5 hours at $1.006/hr.
</Info>

<AccordionGroup>
  <Accordion title="CPU / Apple Silicon path" icon="laptop">
    Modules 1 and 5 are fully runnable — Ollama on Apple Silicon is genuinely fast (35 tok/s for an
    8B at `q4_K_M` on an M2 Pro). Module 3's QLoRA needs CUDA for `bitsandbytes`; use the Colab
    fallback for the training run and come back to your laptop for evaluation and serving.
  </Accordion>
  <Accordion title="Colab T4 free-tier path" icon="cloud">
    QLoRA on a 3B model at `r=8`, `seq_len` 1024, batch 2 with gradient accumulation 8: 38 minutes
    for 3 epochs on 500 examples, peak 13.1 GB of the T4's 15 GB. The rank sweep at `r=64` will not
    fit; do 4/8/16 on Colab and note the omission in your write-up.
  </Accordion>
  <Accordion title="Rented A10G path" icon="server">
    `g5.xlarge`, $1.006/hr. Rank sweep (4 runs) 55 min, serving benchmarks 90 min, quantization
    benchmarks 60 min, plus slack: about 5.5 hours, roughly $5.50. **Shut it down.** The war story
    in Module 5 is about exactly this.
  </Accordion>
</AccordionGroup>

## Prerequisites you should refresh first

<AccordionGroup>
  <Accordion title="From Week 11 — SVD and low-rank approximation" icon="rotate-ccw">
    Singular value decomposition, the Eckart–Young theorem, and the rank-20 image reconstruction.
    Module 3 is that lesson's payoff and will not land without it. If you remember only "SVD is a
    matrix factorisation", spend an hour there first.
  </Accordion>
  <Accordion title="From Week 9 — virtual memory" icon="rotate-ccw">
    Page tables, on-demand allocation, internal and external fragmentation, copy-on-write.
    PagedAttention in Module 5 is these ideas applied unchanged to the KV cache.
  </Accordion>
  <Accordion title="From Week 6 — Pydantic and JSON Schema" icon="rotate-ccw">
    Models, `Field` constraints, validators, and `model_json_schema()`. Module 4 turns your Pydantic
    model into a decoding constraint.
  </Accordion>
  <Accordion title="From Week 12 and 19 — CIs and eval sets" icon="rotate-ccw">
    Bootstrap confidence intervals and how to build an eval set. Half of this week's discipline is
    refusing to report a difference your sample size cannot resolve.
  </Accordion>
</AccordionGroup>

## This week's rhythm

| Day | Student (12–15 h) | Working professional (5–7 h) |
| --- | --- | --- |
| Mon | Module 1 (Layers 1–2), start the quantization benchmark running | Module 1 Layer 2 + the Pareto table |
| Tue | Module 2 in full — build the dataset, this is the long pole | Module 2 Layer 2, write the decision table for a real project |
| Wed | Module 3 (Layers 1–2), first QLoRA run | Module 3 Layers 2–3, run the rank sweep |
| Thu | Module 4, lab `basic` → `standard` | Lab `standard` |
| Fri | Module 5, Layer 3 of Modules 1 and 3 | Module 5 Layer 3 + the break-even arithmetic |
| Sat | Lab `hard`, push to GitHub | Lab `hard` (optional) |
| Sun | Assessment, flashcards, draw-from-memory | Assessment |

<Warning>
**Module 2's dataset is the critical path.** 500 examples is roughly 50 hours of human work if you
do it properly, which does not fit in this week. Start it Monday, timebox it to 8 hours, and accept
200 high-quality examples over 500 mediocre ones — Module 3's Layer 3 table shows 200 curated
examples score 0.84 against 500's 0.89, which is a perfectly good result for a lab.
</Warning>

## What you ship this week

- A quantization Pareto plot with confidence intervals and a capability-stratified breakdown.
- A written fine-tune-or-not decision for a real task, with the cost/latency/quality table.
- A rank sweep with trainable parameters, VRAM, task score and a general-benchmark column.
- A model card and adapter published to Hugging Face, with a reproducible training script.
- A served endpoint with a capacity plan derived from a stated SLO, and a self-host-versus-API
  break-even analysis.

The break-even analysis is the artifact a hiring manager will actually read. It is the one that
shows you can make an engineering decision with money attached.

{/* --- hand-written body below this line is preserved on regeneration --- */}
