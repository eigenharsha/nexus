# LAB-P3-W17 — `scratchml`: regression & classification from NumPy
# Everything reusable lives in lab.mk. Only the four lines below are lab-specific.

LAB_ID      := LAB-P3-W17
LAB_TITLE   := `scratchml`: regression & classification from NumPy
LAB_TIMEBOX := 10-12 h
LANG        := python

BENCH_CMD = PYTHONPATH=$(CURDIR)/$(IMPL) $(PY) $(IMPL)/bench.py

include lab.mk
