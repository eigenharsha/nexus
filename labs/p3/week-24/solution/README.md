# solution/

The reference implementation. `make verify IMPL=solution` is green here by construction, and
`make contract` asserts it stays that way.

Read this **after** you have a failing attempt of your own. Then delete the function you read
and rewrite it from memory — that is the difference between having read a solution and being
able to produce one.
