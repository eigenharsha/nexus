apiVersion: v1
kind: ConfigMap
metadata:
  name: prediction-config
data:
  MODEL_PATH: /app/artifacts/model.pt
  LOG_LEVEL: info
  # TODO: everything that changes between environments belongs here, and nothing
  # that is a secret. If it would be embarrassing in `kubectl get cm -o yaml`, it is
  # a Secret.
