# LAB-P1-W04 — Async Concurrent Downloader
# Everything reusable lives in lab.mk. Only the four lines below are lab-specific.

LAB_ID      := LAB-P1-W04
LAB_TITLE   := Async Concurrent Downloader
LAB_TIMEBOX := 8-10 h
LANG        := python

BENCH_CMD = PYTHONPATH=$(CURDIR)/$(IMPL):$(CURDIR) $(PY) $(IMPL)/bench.py

include lab.mk
