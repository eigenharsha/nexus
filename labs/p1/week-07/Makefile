# LAB-P1-W07 — E-commerce schema & analytics suite
# Everything reusable lives in lab.mk. Only the four lines below are lab-specific.

LAB_ID      := LAB-P1-W07
LAB_TITLE   := E-commerce schema & analytics suite
LAB_TIMEBOX := 10-12 h
LANG        := python

# Uncomment and point at the lab's benchmark entry point where one exists.
# BENCH_CMD = $(PY) solution/bench.py --out bench/results.csv

include lab.mk
