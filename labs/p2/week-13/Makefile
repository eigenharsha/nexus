# LAB-P2-W13 — `pycollections`: structures from scratch + benchmark harness
# Everything reusable lives in lab.mk. Only the four lines below are lab-specific.

LAB_ID      := LAB-P2-W13
LAB_TITLE   := `pycollections`: structures from scratch + benchmark harness
LAB_TIMEBOX := 10-12 h
LANG        := python

BENCH_CMD = PYTHONPATH=$(CURDIR)/$(IMPL) $(PY) $(IMPL)/bench.py

include lab.mk
