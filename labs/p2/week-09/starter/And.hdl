// And.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/And.tst and diffs the output
// against And.cmp.

CHIP And {
    IN a, b;
    OUT out;

    PARTS:
    // TODO: implement And.
}
