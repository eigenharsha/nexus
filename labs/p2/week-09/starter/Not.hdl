// Not.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/Not.tst and diffs the output
// against Not.cmp.

CHIP Not {
    IN in;
    OUT out;

    PARTS:
    // TODO: implement Not.
}
