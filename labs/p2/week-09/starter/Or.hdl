// Or.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/Or.tst and diffs the output
// against Or.cmp.

CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    // TODO: implement Or.
}
