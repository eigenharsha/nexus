// Xor.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/Xor.tst and diffs the output
// against Xor.cmp.

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    // TODO: implement Xor.
}
