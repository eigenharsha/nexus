// DMux.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/DMux.tst and diffs the output
// against DMux.cmp.

CHIP DMux {
    IN in, sel;
    OUT a, b;

    PARTS:
    // TODO: implement DMux.
}
