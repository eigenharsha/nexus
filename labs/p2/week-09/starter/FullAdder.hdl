// FullAdder.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/FullAdder.tst and diffs the output
// against FullAdder.cmp.

CHIP FullAdder {
    IN a, b, c;
    OUT sum, carry;

    PARTS:
    // TODO: implement FullAdder.
}
