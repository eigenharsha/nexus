// HalfAdder.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/HalfAdder.tst and diffs the output
// against HalfAdder.cmp.

CHIP HalfAdder {
    IN a, b;
    OUT sum, carry;

    PARTS:
    // TODO: implement HalfAdder.
}
