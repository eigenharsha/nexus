// Mux.hdl — basic track.
//
// Build it from Nand (and from chips you have already built). Nothing else.
// `make verify TRACK=basic` runs tests/scripts/Mux.tst and diffs the output
// against Mux.cmp.

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    // TODO: implement Mux.
}
