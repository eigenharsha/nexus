---
title: "Week 17 — Supervised Learning from Scratch"
description: "learner derives, implements and trains linear/logistic regression without a library, then reproduces it in scikit-learn and knows what the library was"
icon: "calendar"
---
{/* AUTHORED */}

## Outcome

<Note>By Sunday you will have derived, implemented and trained linear and logistic regression without a library, reproduced both in scikit-learn, and be able to explain every difference between your numbers and the library's.</Note>

## The through-line

Five modules, one argument. **M1** says a model is a function chosen from a family by minimising a loss on data you are allowed to see. **M2** picks the simplest family — a line — derives its gradient, and shows that feature scale decides whether training converges at all (condition number `5.6e10` down to `42.8`, and a learning rate that diverges to `inf` becomes one that reaches the optimum). **M3** swaps the output through a sigmoid and the loss to cross-entropy, derives that gradient too, and proves why mean squared error is the wrong partner for a sigmoid — the curvature goes negative and the gradient is **203x** too small exactly where the model is most wrong. **M4** adds a penalty and measures the bias-variance trade-off directly: ridge cuts a degree-15 polynomial's variance from **21,765 to 0.198**. **M5** hands the whole thing to scikit-learn and shows the one structural mistake that turns pure noise into **81.5% cross-validated accuracy**.

Every gradient you derive this week reappears in Week 21 as a layer of a neural network. That is not a metaphor: `X^T (p - y)` is literally the backward pass of a linear layer with a cross-entropy head.

## Modules

<CardGroup cols={2}>
  <Card title="1 · What learning from data actually is" icon="pen-line" href="/curriculum/p3/week-17/1-what-learning-from-data-actually-is">Rows, labels, hypothesis, loss, the wall — and the framing checklist that kills a third of ML projects before they start.</Card>
  <Card title="2 · Linear regression from mathematical scratch" icon="pen-line" href="/curriculum/p3/week-17/2-linear-regression-from-mathematical-scratch">Derive the MSE gradient, implement two solvers, and find the crossover between them in time and in memory.</Card>
  <Card title="3 · Logistic regression & classification" icon="pen-line" href="/curriculum/p3/week-17/3-logistic-regression-classification">Sigmoid, odds, cross-entropy from maximum likelihood, a gradient check to 1e-10, and why 0.5 is not a decision.</Card>
  <Card title="4 · Regularization & the bias-variance trade-off" icon="pen-line" href="/curriculum/p3/week-17/4-regularization-the-bias-variance-trade-off">Measure bias, variance and the noise floor; derive soft thresholding; reproduce double descent.</Card>
  <Card title="5 · scikit-learn: the industry surface" icon="pen-line" href="/curriculum/p3/week-17/5-scikit-learn-the-industry-surface">Estimators, pipelines, `ColumnTransformer`, custom transformers, solvers, and the 30.5-point leak.</Card>
</CardGroup>

<Card title="Lab — scratchml" icon="flask-conical" href="/curriculum/p3/week-17/lab">
  Regression and classification from NumPy, with scikit-learn-compatible APIs and gradient checks. Three tracks.
</Card>

## The five numbers to leave the week with

| Number | What it is | Where |
| --- | --- | --- |
| **20,640x** | compression of the California housing data into a 9-parameter model | M1 |
| **5.6e10 to 42.8** | condition number of `X'X` before and after standardisation; the difference between diverging and converging | M2 |
| **203x** | how much larger the cross-entropy gradient is than the MSE gradient on a confidently-wrong example | M3 |
| **21,765 to 0.198** | variance of a degree-15 polynomial fit before and after `Ridge(alpha=1)` | M4 |
| **0.815 vs 0.510** | cross-validated accuracy on pure noise, with and without a `Pipeline` | M5 |

## Prerequisites check

Before Monday you should be able to: multiply two matrices by hand and say the output shape; take a partial derivative with the chain rule; explain what a standard deviation is; and write a `for` loop in Python that accumulates a running total. If any of those is shaky, revisit Week 11 (linear algebra and calculus) and Week 12 (probability) first — this week goes fast and does not re-teach them.

## Environment

```bash
uv venv --python 3.12 .venv && source .venv/bin/activate
uv pip install "numpy==2.5.2" "scikit-learn==1.9.0" "scipy==1.18.1" \
               "pandas==3.0.5" "matplotlib==3.11.1" "pytest==9.1.1"
```

Every number quoted in this week's pages was **measured** on an Apple M3 Pro (11 cores, 18 GB) with exactly these versions and Python 3.12.9 — the machine this course was written on, running the versions pinned above. Your absolute timings will differ; the ratios should not. The bias-variance decomposition and the normal-equation cost model are **derived** rather than timed, and hold on any machine. See [About the numbers](/start/about-the-numbers).

## This week's rhythm

| Day | Student (12–15 h) | Working professional (5–7 h) |
| --- | --- | --- |
| Mon–Tue | M1 + M2, both layers 1 and 2 | M2 layer 2, M3 layer 2 |
| Wed–Thu | Lab `basic` → `standard` | Lab `standard` |
| Fri | M3 + M4 layer 3, the double-descent stretch | M4 + M5 layer 3 skim |
| Sat | M5, then lab `hard`, push to GitHub | Lab `hard` (optional) |
| Sun | Assessment + flashcards + draw-from-memory | Assessment |

## Deliverables

- A `scratchml` repository with both solvers, gradient checks and a comparison against scikit-learn on three datasets
- One crossover plot (exact solve vs SGD) annotated with the `8*p^2` memory number
- One double-descent plot with `||beta||` overlaid
- A written derivation post covering the MSE gradient and the cross-entropy gradient — this is the artifact that gets read in interviews
- Five framing documents from M1's hands-on, two of which reject the request

{/* --- hand-written body below this line is preserved on regeneration --- */}
