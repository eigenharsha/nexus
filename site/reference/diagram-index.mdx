---
title: "Diagram index"
description: "Every sketch, build-up, animation and interactive widget in the course."
icon: "pen-line"
---

Every diagram in Nexus is hand-drawn in the same house style: whiteboard marker weight, visible
wobble, rough work left in, mistakes crossed out in red rather than erased, and the teacher's
margin notes in amber. Sources are Excalidraw files you can open and edit.

<CardGroup cols={4}>
  <Card title="T1 · Sketch" icon="pen-line">One static hand-drawn SVG. **Every module has at least one.**</Card>
  <Card title="T2 · Build-up" icon="layers">3–5 frames that assemble as the explanation proceeds.</Card>
  <Card title="T3 · Animation" icon="play">True motion. 42 hero concepts.</Card>
  <Card title="T4 · Interactive" icon="sliders">You move a parameter, the picture responds. 15 widgets.</Card>
</CardGroup>

## The 42 hero animations

| # | Week | Animation | What it shows |
| --- | --- | --- | --- |
| A01 | 1 | Stack vs heap through a call chain | frames pushing and popping; a `malloc`'d block outliving its frame; the dangling pointer turning red |
| A02 | 1 | Merge sort | the array splitting down and merging back up, comparison counter ticking |
| A03 | 1 | Binary search | the window halving, invariant written beside each step |
| A04 | 2 | A shell pipeline | text transforming as it flows through `grep → sort → uniq` |
| A05 | 2 | Git commit DAG | merge vs rebase, side by side, from the same starting graph |
| A06 | 3 | Names, objects, refcounts | two names on one list; mutation seen through both |
| A07 | 4 | Blocking vs async I/O | two timelines: waiting vs serving other tables |
| A08 | 4 | The event loop | tasks queueing, awaiting, resuming |
| A09 | 5 | HTTP request lifecycle | DNS → TCP → TLS → request → response, with time on each segment |
| A10 | 6 | Pydantic validation | a bad payload bouncing off the schema, field by field |
| A11 | 7 | JOIN types | rows physically combining, one JOIN at a time |
| A12 | 8 | B-Tree lookup vs full scan | descending the tree while a sequential scan races alongside and loses |
| A13 | 8 | Two concurrent transactions | the lost update happening live, then the lock preventing it |
| A14 | 9 | NAND → adder → 16-bit adder | gates composing upward, carry rippling |
| A15 | 9 | Memory hierarchy | a request falling through L1 → DRAM → SSD with latency bars to scale |
| A16 | 10 | TCP handshake + retransmission | packets crossing, one lost, the retry firing |
| A17 | 11 | Matrix as transformation | a shape rotating and shearing as the numbers change |
| A18 | 11 | Gradient descent | the ball rolling; too-high LR diverging; momentum overshooting |
| A19 | 11 | Low-rank approximation | an image degrading as rank falls — the visual that makes LoRA obvious |
| A20 | 12 | Central limit theorem | sample means piling into a normal curve |
| A21 | 13 | Hash table insert, collide, resize | keys landing in buckets, a chain forming, a rehash |
| A22 | 14 | Dijkstra | the frontier expanding, distances updating |
| A23 | 14 | DP table filling | the grid populating with the recurrence shown |
| A24 | 15 | Incremental pipeline run | only new rows merging in, the watermark advancing |
| A25 | 17 | Logistic regression training | the decision boundary rotating into place as loss falls |
| A26 | 18 | Gradient boosting | residuals shrinking as trees are added |
| A27 | 19 | Threshold sliding on a PR curve | the confusion matrix updating live |
| A28 | 21 | **Backpropagation** | forward pass, loss, gradients flowing backwards, weights nudging |
| A29 | 22 | Convolution | the kernel sliding, the feature map filling in |
| A30 | 24 | Kubernetes autoscaling | load rising, pods spawning, latency recovering — lag windows marked |
| A31 | 25 | BPE merges | the merge table growing, a word's tokenization changing |
| A32 | 25 | **Self-attention** | the matrix filling token by token, the causal mask blocking the future |
| A33 | 25 | KV cache | the cache growing per token; recompute vs cache side by side |
| A34 | 26 | Quantization | a float squeezed 32 → 16 → 8 → 4, error accumulating |
| A35 | 26 | LoRA | the frozen matrix with a thin B·A path lighting up, parameter counts ticking |
| A36 | 27 | Chunking strategies | one document split three ways, retrieved unit highlighted |
| A37 | 27 | **HNSW search** | a query entering the top layer and hopping greedily down |
| A38 | 28 | Hybrid fusion | two ranked lists merging, the right answer climbing to #1 |
| A39 | 29 | ReAct loop | thought → action → observation cycling, scratchpad filling |
| A40 | 30 | Checkpoint & resume | the graph running, the process dying, resuming from the last checkpoint |
| A41 | 31 | Prompt injection | a hidden instruction inside a retrieved document being obeyed — trust boundary breached in red |
| A42 | 32 | A distributed trace | spans nesting into a waterfall, cost accumulating |

## The 15 interactive widgets

| Week | Widget | You control |
| --- | --- | --- |
| 1 | Big-O grower | n, and watch the curves diverge |
| 11 | Gradient descent playground | learning rate, momentum, start point |
| 11 | Vector similarity | drag two vectors; cosine and Euclidean disagree |
| 12 | Sampling & CLT | sample size, underlying distribution |
| 17 | Bias–variance | polynomial degree on fixed data |
| 19 | Threshold explorer | the threshold → confusion matrix and cost |
| 21 | Neural net playground | depth, width, activation on 2-D data |
| 22 | Convolution kernel editor | the 3×3 kernel, live on an image |
| 25 | Tokenizer explorer | your text, across four tokenizers |
| 25 | Attention inspector | pick a token, see what it attends to |
| 25 | Sampling parameters | temperature, top-k, top-p on a real distribution |
| 26 | VRAM calculator | model size, precision, batch, context → "does it fit?" |
| 27 | HNSW explorer | M and ef → recall vs latency |
| 28 | RAG cost calculator | chunk size, top-k, rerank depth → $/query and latency |
| 32 | Cost attribution | tokens per step → the cost waterfall |

<Tip>
**Draw it yourself.** Every module ships a blank, unlabelled version of its key diagram.
Reproducing it from memory is graded, every week, because it's the cheapest high-retention
exercise in the course.
</Tip>
